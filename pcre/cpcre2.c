// SPDX-FileCopyrightText: 2025 ModelCloud.ai
// SPDX-FileCopyrightText: 2025 qubitium@modelcloud.ai
// SPDX-License-Identifier: Apache-2.0
// Contact: qubitium@modelcloud.ai, x.com/qubitium

#define PY_SSIZE_T_CLEAN
#include <Python.h>
#include <structmember.h>
#include "pythread.h"
#include <stdint.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>

#define PCRE2_CODE_UNIT_WIDTH 8
#include "pcre2.h"

/* Autogenerated from PCRE2 commit c5ad23c8833b99c56a2913f86dbe16a79a1791fe. */
#ifdef PCRE2_ERROR_END_BACKSLASH
#  define HANDLE_PCRE2_ERROR_END_BACKSLASH(X) X(PCRE2_ERROR_END_BACKSLASH)
#else
#  define HANDLE_PCRE2_ERROR_END_BACKSLASH(X)
#endif
#ifdef PCRE2_ERROR_END_BACKSLASH_C
#  define HANDLE_PCRE2_ERROR_END_BACKSLASH_C(X) X(PCRE2_ERROR_END_BACKSLASH_C)
#else
#  define HANDLE_PCRE2_ERROR_END_BACKSLASH_C(X)
#endif
#ifdef PCRE2_ERROR_UNKNOWN_ESCAPE
#  define HANDLE_PCRE2_ERROR_UNKNOWN_ESCAPE(X) X(PCRE2_ERROR_UNKNOWN_ESCAPE)
#else
#  define HANDLE_PCRE2_ERROR_UNKNOWN_ESCAPE(X)
#endif
#ifdef PCRE2_ERROR_QUANTIFIER_OUT_OF_ORDER
#  define HANDLE_PCRE2_ERROR_QUANTIFIER_OUT_OF_ORDER(X) X(PCRE2_ERROR_QUANTIFIER_OUT_OF_ORDER)
#else
#  define HANDLE_PCRE2_ERROR_QUANTIFIER_OUT_OF_ORDER(X)
#endif
#ifdef PCRE2_ERROR_QUANTIFIER_TOO_BIG
#  define HANDLE_PCRE2_ERROR_QUANTIFIER_TOO_BIG(X) X(PCRE2_ERROR_QUANTIFIER_TOO_BIG)
#else
#  define HANDLE_PCRE2_ERROR_QUANTIFIER_TOO_BIG(X)
#endif
#ifdef PCRE2_ERROR_MISSING_SQUARE_BRACKET
#  define HANDLE_PCRE2_ERROR_MISSING_SQUARE_BRACKET(X) X(PCRE2_ERROR_MISSING_SQUARE_BRACKET)
#else
#  define HANDLE_PCRE2_ERROR_MISSING_SQUARE_BRACKET(X)
#endif
#ifdef PCRE2_ERROR_ESCAPE_INVALID_IN_CLASS
#  define HANDLE_PCRE2_ERROR_ESCAPE_INVALID_IN_CLASS(X) X(PCRE2_ERROR_ESCAPE_INVALID_IN_CLASS)
#else
#  define HANDLE_PCRE2_ERROR_ESCAPE_INVALID_IN_CLASS(X)
#endif
#ifdef PCRE2_ERROR_CLASS_RANGE_ORDER
#  define HANDLE_PCRE2_ERROR_CLASS_RANGE_ORDER(X) X(PCRE2_ERROR_CLASS_RANGE_ORDER)
#else
#  define HANDLE_PCRE2_ERROR_CLASS_RANGE_ORDER(X)
#endif
#ifdef PCRE2_ERROR_QUANTIFIER_INVALID
#  define HANDLE_PCRE2_ERROR_QUANTIFIER_INVALID(X) X(PCRE2_ERROR_QUANTIFIER_INVALID)
#else
#  define HANDLE_PCRE2_ERROR_QUANTIFIER_INVALID(X)
#endif
#ifdef PCRE2_ERROR_INTERNAL_UNEXPECTED_REPEAT
#  define HANDLE_PCRE2_ERROR_INTERNAL_UNEXPECTED_REPEAT(X) X(PCRE2_ERROR_INTERNAL_UNEXPECTED_REPEAT)
#else
#  define HANDLE_PCRE2_ERROR_INTERNAL_UNEXPECTED_REPEAT(X)
#endif
#ifdef PCRE2_ERROR_INVALID_AFTER_PARENS_QUERY
#  define HANDLE_PCRE2_ERROR_INVALID_AFTER_PARENS_QUERY(X) X(PCRE2_ERROR_INVALID_AFTER_PARENS_QUERY)
#else
#  define HANDLE_PCRE2_ERROR_INVALID_AFTER_PARENS_QUERY(X)
#endif
#ifdef PCRE2_ERROR_POSIX_CLASS_NOT_IN_CLASS
#  define HANDLE_PCRE2_ERROR_POSIX_CLASS_NOT_IN_CLASS(X) X(PCRE2_ERROR_POSIX_CLASS_NOT_IN_CLASS)
#else
#  define HANDLE_PCRE2_ERROR_POSIX_CLASS_NOT_IN_CLASS(X)
#endif
#ifdef PCRE2_ERROR_POSIX_NO_SUPPORT_COLLATING
#  define HANDLE_PCRE2_ERROR_POSIX_NO_SUPPORT_COLLATING(X) X(PCRE2_ERROR_POSIX_NO_SUPPORT_COLLATING)
#else
#  define HANDLE_PCRE2_ERROR_POSIX_NO_SUPPORT_COLLATING(X)
#endif
#ifdef PCRE2_ERROR_MISSING_CLOSING_PARENTHESIS
#  define HANDLE_PCRE2_ERROR_MISSING_CLOSING_PARENTHESIS(X) X(PCRE2_ERROR_MISSING_CLOSING_PARENTHESIS)
#else
#  define HANDLE_PCRE2_ERROR_MISSING_CLOSING_PARENTHESIS(X)
#endif
#ifdef PCRE2_ERROR_BAD_SUBPATTERN_REFERENCE
#  define HANDLE_PCRE2_ERROR_BAD_SUBPATTERN_REFERENCE(X) X(PCRE2_ERROR_BAD_SUBPATTERN_REFERENCE)
#else
#  define HANDLE_PCRE2_ERROR_BAD_SUBPATTERN_REFERENCE(X)
#endif
#ifdef PCRE2_ERROR_NULL_PATTERN
#  define HANDLE_PCRE2_ERROR_NULL_PATTERN(X) X(PCRE2_ERROR_NULL_PATTERN)
#else
#  define HANDLE_PCRE2_ERROR_NULL_PATTERN(X)
#endif
#ifdef PCRE2_ERROR_BAD_OPTIONS
#  define HANDLE_PCRE2_ERROR_BAD_OPTIONS(X) X(PCRE2_ERROR_BAD_OPTIONS)
#else
#  define HANDLE_PCRE2_ERROR_BAD_OPTIONS(X)
#endif
#ifdef PCRE2_ERROR_MISSING_COMMENT_CLOSING
#  define HANDLE_PCRE2_ERROR_MISSING_COMMENT_CLOSING(X) X(PCRE2_ERROR_MISSING_COMMENT_CLOSING)
#else
#  define HANDLE_PCRE2_ERROR_MISSING_COMMENT_CLOSING(X)
#endif
#ifdef PCRE2_ERROR_PARENTHESES_NEST_TOO_DEEP
#  define HANDLE_PCRE2_ERROR_PARENTHESES_NEST_TOO_DEEP(X) X(PCRE2_ERROR_PARENTHESES_NEST_TOO_DEEP)
#else
#  define HANDLE_PCRE2_ERROR_PARENTHESES_NEST_TOO_DEEP(X)
#endif
#ifdef PCRE2_ERROR_PATTERN_TOO_LARGE
#  define HANDLE_PCRE2_ERROR_PATTERN_TOO_LARGE(X) X(PCRE2_ERROR_PATTERN_TOO_LARGE)
#else
#  define HANDLE_PCRE2_ERROR_PATTERN_TOO_LARGE(X)
#endif
#ifdef PCRE2_ERROR_HEAP_FAILED
#  define HANDLE_PCRE2_ERROR_HEAP_FAILED(X) X(PCRE2_ERROR_HEAP_FAILED)
#else
#  define HANDLE_PCRE2_ERROR_HEAP_FAILED(X)
#endif
#ifdef PCRE2_ERROR_UNMATCHED_CLOSING_PARENTHESIS
#  define HANDLE_PCRE2_ERROR_UNMATCHED_CLOSING_PARENTHESIS(X) X(PCRE2_ERROR_UNMATCHED_CLOSING_PARENTHESIS)
#else
#  define HANDLE_PCRE2_ERROR_UNMATCHED_CLOSING_PARENTHESIS(X)
#endif
#ifdef PCRE2_ERROR_INTERNAL_CODE_OVERFLOW
#  define HANDLE_PCRE2_ERROR_INTERNAL_CODE_OVERFLOW(X) X(PCRE2_ERROR_INTERNAL_CODE_OVERFLOW)
#else
#  define HANDLE_PCRE2_ERROR_INTERNAL_CODE_OVERFLOW(X)
#endif
#ifdef PCRE2_ERROR_MISSING_CONDITION_CLOSING
#  define HANDLE_PCRE2_ERROR_MISSING_CONDITION_CLOSING(X) X(PCRE2_ERROR_MISSING_CONDITION_CLOSING)
#else
#  define HANDLE_PCRE2_ERROR_MISSING_CONDITION_CLOSING(X)
#endif
#ifdef PCRE2_ERROR_LOOKBEHIND_NOT_FIXED_LENGTH
#  define HANDLE_PCRE2_ERROR_LOOKBEHIND_NOT_FIXED_LENGTH(X) X(PCRE2_ERROR_LOOKBEHIND_NOT_FIXED_LENGTH)
#else
#  define HANDLE_PCRE2_ERROR_LOOKBEHIND_NOT_FIXED_LENGTH(X)
#endif
#ifdef PCRE2_ERROR_ZERO_RELATIVE_REFERENCE
#  define HANDLE_PCRE2_ERROR_ZERO_RELATIVE_REFERENCE(X) X(PCRE2_ERROR_ZERO_RELATIVE_REFERENCE)
#else
#  define HANDLE_PCRE2_ERROR_ZERO_RELATIVE_REFERENCE(X)
#endif
#ifdef PCRE2_ERROR_TOO_MANY_CONDITION_BRANCHES
#  define HANDLE_PCRE2_ERROR_TOO_MANY_CONDITION_BRANCHES(X) X(PCRE2_ERROR_TOO_MANY_CONDITION_BRANCHES)
#else
#  define HANDLE_PCRE2_ERROR_TOO_MANY_CONDITION_BRANCHES(X)
#endif
#ifdef PCRE2_ERROR_CONDITION_ASSERTION_EXPECTED
#  define HANDLE_PCRE2_ERROR_CONDITION_ASSERTION_EXPECTED(X) X(PCRE2_ERROR_CONDITION_ASSERTION_EXPECTED)
#else
#  define HANDLE_PCRE2_ERROR_CONDITION_ASSERTION_EXPECTED(X)
#endif
#ifdef PCRE2_ERROR_BAD_RELATIVE_REFERENCE
#  define HANDLE_PCRE2_ERROR_BAD_RELATIVE_REFERENCE(X) X(PCRE2_ERROR_BAD_RELATIVE_REFERENCE)
#else
#  define HANDLE_PCRE2_ERROR_BAD_RELATIVE_REFERENCE(X)
#endif
#ifdef PCRE2_ERROR_UNKNOWN_POSIX_CLASS
#  define HANDLE_PCRE2_ERROR_UNKNOWN_POSIX_CLASS(X) X(PCRE2_ERROR_UNKNOWN_POSIX_CLASS)
#else
#  define HANDLE_PCRE2_ERROR_UNKNOWN_POSIX_CLASS(X)
#endif
#ifdef PCRE2_ERROR_INTERNAL_STUDY_ERROR
#  define HANDLE_PCRE2_ERROR_INTERNAL_STUDY_ERROR(X) X(PCRE2_ERROR_INTERNAL_STUDY_ERROR)
#else
#  define HANDLE_PCRE2_ERROR_INTERNAL_STUDY_ERROR(X)
#endif
#ifdef PCRE2_ERROR_UNICODE_NOT_SUPPORTED
#  define HANDLE_PCRE2_ERROR_UNICODE_NOT_SUPPORTED(X) X(PCRE2_ERROR_UNICODE_NOT_SUPPORTED)
#else
#  define HANDLE_PCRE2_ERROR_UNICODE_NOT_SUPPORTED(X)
#endif
#ifdef PCRE2_ERROR_PARENTHESES_STACK_CHECK
#  define HANDLE_PCRE2_ERROR_PARENTHESES_STACK_CHECK(X) X(PCRE2_ERROR_PARENTHESES_STACK_CHECK)
#else
#  define HANDLE_PCRE2_ERROR_PARENTHESES_STACK_CHECK(X)
#endif
#ifdef PCRE2_ERROR_CODE_POINT_TOO_BIG
#  define HANDLE_PCRE2_ERROR_CODE_POINT_TOO_BIG(X) X(PCRE2_ERROR_CODE_POINT_TOO_BIG)
#else
#  define HANDLE_PCRE2_ERROR_CODE_POINT_TOO_BIG(X)
#endif
#ifdef PCRE2_ERROR_LOOKBEHIND_TOO_COMPLICATED
#  define HANDLE_PCRE2_ERROR_LOOKBEHIND_TOO_COMPLICATED(X) X(PCRE2_ERROR_LOOKBEHIND_TOO_COMPLICATED)
#else
#  define HANDLE_PCRE2_ERROR_LOOKBEHIND_TOO_COMPLICATED(X)
#endif
#ifdef PCRE2_ERROR_LOOKBEHIND_INVALID_BACKSLASH_C
#  define HANDLE_PCRE2_ERROR_LOOKBEHIND_INVALID_BACKSLASH_C(X) X(PCRE2_ERROR_LOOKBEHIND_INVALID_BACKSLASH_C)
#else
#  define HANDLE_PCRE2_ERROR_LOOKBEHIND_INVALID_BACKSLASH_C(X)
#endif
#ifdef PCRE2_ERROR_UNSUPPORTED_ESCAPE_SEQUENCE
#  define HANDLE_PCRE2_ERROR_UNSUPPORTED_ESCAPE_SEQUENCE(X) X(PCRE2_ERROR_UNSUPPORTED_ESCAPE_SEQUENCE)
#else
#  define HANDLE_PCRE2_ERROR_UNSUPPORTED_ESCAPE_SEQUENCE(X)
#endif
#ifdef PCRE2_ERROR_CALLOUT_NUMBER_TOO_BIG
#  define HANDLE_PCRE2_ERROR_CALLOUT_NUMBER_TOO_BIG(X) X(PCRE2_ERROR_CALLOUT_NUMBER_TOO_BIG)
#else
#  define HANDLE_PCRE2_ERROR_CALLOUT_NUMBER_TOO_BIG(X)
#endif
#ifdef PCRE2_ERROR_MISSING_CALLOUT_CLOSING
#  define HANDLE_PCRE2_ERROR_MISSING_CALLOUT_CLOSING(X) X(PCRE2_ERROR_MISSING_CALLOUT_CLOSING)
#else
#  define HANDLE_PCRE2_ERROR_MISSING_CALLOUT_CLOSING(X)
#endif
#ifdef PCRE2_ERROR_ESCAPE_INVALID_IN_VERB
#  define HANDLE_PCRE2_ERROR_ESCAPE_INVALID_IN_VERB(X) X(PCRE2_ERROR_ESCAPE_INVALID_IN_VERB)
#else
#  define HANDLE_PCRE2_ERROR_ESCAPE_INVALID_IN_VERB(X)
#endif
#ifdef PCRE2_ERROR_UNRECOGNIZED_AFTER_QUERY_P
#  define HANDLE_PCRE2_ERROR_UNRECOGNIZED_AFTER_QUERY_P(X) X(PCRE2_ERROR_UNRECOGNIZED_AFTER_QUERY_P)
#else
#  define HANDLE_PCRE2_ERROR_UNRECOGNIZED_AFTER_QUERY_P(X)
#endif
#ifdef PCRE2_ERROR_MISSING_NAME_TERMINATOR
#  define HANDLE_PCRE2_ERROR_MISSING_NAME_TERMINATOR(X) X(PCRE2_ERROR_MISSING_NAME_TERMINATOR)
#else
#  define HANDLE_PCRE2_ERROR_MISSING_NAME_TERMINATOR(X)
#endif
#ifdef PCRE2_ERROR_DUPLICATE_SUBPATTERN_NAME
#  define HANDLE_PCRE2_ERROR_DUPLICATE_SUBPATTERN_NAME(X) X(PCRE2_ERROR_DUPLICATE_SUBPATTERN_NAME)
#else
#  define HANDLE_PCRE2_ERROR_DUPLICATE_SUBPATTERN_NAME(X)
#endif
#ifdef PCRE2_ERROR_INVALID_SUBPATTERN_NAME
#  define HANDLE_PCRE2_ERROR_INVALID_SUBPATTERN_NAME(X) X(PCRE2_ERROR_INVALID_SUBPATTERN_NAME)
#else
#  define HANDLE_PCRE2_ERROR_INVALID_SUBPATTERN_NAME(X)
#endif
#ifdef PCRE2_ERROR_UNICODE_PROPERTIES_UNAVAILABLE
#  define HANDLE_PCRE2_ERROR_UNICODE_PROPERTIES_UNAVAILABLE(X) X(PCRE2_ERROR_UNICODE_PROPERTIES_UNAVAILABLE)
#else
#  define HANDLE_PCRE2_ERROR_UNICODE_PROPERTIES_UNAVAILABLE(X)
#endif
#ifdef PCRE2_ERROR_MALFORMED_UNICODE_PROPERTY
#  define HANDLE_PCRE2_ERROR_MALFORMED_UNICODE_PROPERTY(X) X(PCRE2_ERROR_MALFORMED_UNICODE_PROPERTY)
#else
#  define HANDLE_PCRE2_ERROR_MALFORMED_UNICODE_PROPERTY(X)
#endif
#ifdef PCRE2_ERROR_UNKNOWN_UNICODE_PROPERTY
#  define HANDLE_PCRE2_ERROR_UNKNOWN_UNICODE_PROPERTY(X) X(PCRE2_ERROR_UNKNOWN_UNICODE_PROPERTY)
#else
#  define HANDLE_PCRE2_ERROR_UNKNOWN_UNICODE_PROPERTY(X)
#endif
#ifdef PCRE2_ERROR_SUBPATTERN_NAME_TOO_LONG
#  define HANDLE_PCRE2_ERROR_SUBPATTERN_NAME_TOO_LONG(X) X(PCRE2_ERROR_SUBPATTERN_NAME_TOO_LONG)
#else
#  define HANDLE_PCRE2_ERROR_SUBPATTERN_NAME_TOO_LONG(X)
#endif
#ifdef PCRE2_ERROR_TOO_MANY_NAMED_SUBPATTERNS
#  define HANDLE_PCRE2_ERROR_TOO_MANY_NAMED_SUBPATTERNS(X) X(PCRE2_ERROR_TOO_MANY_NAMED_SUBPATTERNS)
#else
#  define HANDLE_PCRE2_ERROR_TOO_MANY_NAMED_SUBPATTERNS(X)
#endif
#ifdef PCRE2_ERROR_CLASS_INVALID_RANGE
#  define HANDLE_PCRE2_ERROR_CLASS_INVALID_RANGE(X) X(PCRE2_ERROR_CLASS_INVALID_RANGE)
#else
#  define HANDLE_PCRE2_ERROR_CLASS_INVALID_RANGE(X)
#endif
#ifdef PCRE2_ERROR_OCTAL_BYTE_TOO_BIG
#  define HANDLE_PCRE2_ERROR_OCTAL_BYTE_TOO_BIG(X) X(PCRE2_ERROR_OCTAL_BYTE_TOO_BIG)
#else
#  define HANDLE_PCRE2_ERROR_OCTAL_BYTE_TOO_BIG(X)
#endif
#ifdef PCRE2_ERROR_INTERNAL_OVERRAN_WORKSPACE
#  define HANDLE_PCRE2_ERROR_INTERNAL_OVERRAN_WORKSPACE(X) X(PCRE2_ERROR_INTERNAL_OVERRAN_WORKSPACE)
#else
#  define HANDLE_PCRE2_ERROR_INTERNAL_OVERRAN_WORKSPACE(X)
#endif
#ifdef PCRE2_ERROR_INTERNAL_MISSING_SUBPATTERN
#  define HANDLE_PCRE2_ERROR_INTERNAL_MISSING_SUBPATTERN(X) X(PCRE2_ERROR_INTERNAL_MISSING_SUBPATTERN)
#else
#  define HANDLE_PCRE2_ERROR_INTERNAL_MISSING_SUBPATTERN(X)
#endif
#ifdef PCRE2_ERROR_DEFINE_TOO_MANY_BRANCHES
#  define HANDLE_PCRE2_ERROR_DEFINE_TOO_MANY_BRANCHES(X) X(PCRE2_ERROR_DEFINE_TOO_MANY_BRANCHES)
#else
#  define HANDLE_PCRE2_ERROR_DEFINE_TOO_MANY_BRANCHES(X)
#endif
#ifdef PCRE2_ERROR_BACKSLASH_O_MISSING_BRACE
#  define HANDLE_PCRE2_ERROR_BACKSLASH_O_MISSING_BRACE(X) X(PCRE2_ERROR_BACKSLASH_O_MISSING_BRACE)
#else
#  define HANDLE_PCRE2_ERROR_BACKSLASH_O_MISSING_BRACE(X)
#endif
#ifdef PCRE2_ERROR_INTERNAL_UNKNOWN_NEWLINE
#  define HANDLE_PCRE2_ERROR_INTERNAL_UNKNOWN_NEWLINE(X) X(PCRE2_ERROR_INTERNAL_UNKNOWN_NEWLINE)
#else
#  define HANDLE_PCRE2_ERROR_INTERNAL_UNKNOWN_NEWLINE(X)
#endif
#ifdef PCRE2_ERROR_BACKSLASH_G_SYNTAX
#  define HANDLE_PCRE2_ERROR_BACKSLASH_G_SYNTAX(X) X(PCRE2_ERROR_BACKSLASH_G_SYNTAX)
#else
#  define HANDLE_PCRE2_ERROR_BACKSLASH_G_SYNTAX(X)
#endif
#ifdef PCRE2_ERROR_PARENS_QUERY_R_MISSING_CLOSING
#  define HANDLE_PCRE2_ERROR_PARENS_QUERY_R_MISSING_CLOSING(X) X(PCRE2_ERROR_PARENS_QUERY_R_MISSING_CLOSING)
#else
#  define HANDLE_PCRE2_ERROR_PARENS_QUERY_R_MISSING_CLOSING(X)
#endif
#ifdef PCRE2_ERROR_VERB_ARGUMENT_NOT_ALLOWED
#  define HANDLE_PCRE2_ERROR_VERB_ARGUMENT_NOT_ALLOWED(X) X(PCRE2_ERROR_VERB_ARGUMENT_NOT_ALLOWED)
#else
#  define HANDLE_PCRE2_ERROR_VERB_ARGUMENT_NOT_ALLOWED(X)
#endif
#ifdef PCRE2_ERROR_VERB_UNKNOWN
#  define HANDLE_PCRE2_ERROR_VERB_UNKNOWN(X) X(PCRE2_ERROR_VERB_UNKNOWN)
#else
#  define HANDLE_PCRE2_ERROR_VERB_UNKNOWN(X)
#endif
#ifdef PCRE2_ERROR_SUBPATTERN_NUMBER_TOO_BIG
#  define HANDLE_PCRE2_ERROR_SUBPATTERN_NUMBER_TOO_BIG(X) X(PCRE2_ERROR_SUBPATTERN_NUMBER_TOO_BIG)
#else
#  define HANDLE_PCRE2_ERROR_SUBPATTERN_NUMBER_TOO_BIG(X)
#endif
#ifdef PCRE2_ERROR_SUBPATTERN_NAME_EXPECTED
#  define HANDLE_PCRE2_ERROR_SUBPATTERN_NAME_EXPECTED(X) X(PCRE2_ERROR_SUBPATTERN_NAME_EXPECTED)
#else
#  define HANDLE_PCRE2_ERROR_SUBPATTERN_NAME_EXPECTED(X)
#endif
#ifdef PCRE2_ERROR_INTERNAL_PARSED_OVERFLOW
#  define HANDLE_PCRE2_ERROR_INTERNAL_PARSED_OVERFLOW(X) X(PCRE2_ERROR_INTERNAL_PARSED_OVERFLOW)
#else
#  define HANDLE_PCRE2_ERROR_INTERNAL_PARSED_OVERFLOW(X)
#endif
#ifdef PCRE2_ERROR_INVALID_OCTAL
#  define HANDLE_PCRE2_ERROR_INVALID_OCTAL(X) X(PCRE2_ERROR_INVALID_OCTAL)
#else
#  define HANDLE_PCRE2_ERROR_INVALID_OCTAL(X)
#endif
#ifdef PCRE2_ERROR_SUBPATTERN_NAMES_MISMATCH
#  define HANDLE_PCRE2_ERROR_SUBPATTERN_NAMES_MISMATCH(X) X(PCRE2_ERROR_SUBPATTERN_NAMES_MISMATCH)
#else
#  define HANDLE_PCRE2_ERROR_SUBPATTERN_NAMES_MISMATCH(X)
#endif
#ifdef PCRE2_ERROR_MARK_MISSING_ARGUMENT
#  define HANDLE_PCRE2_ERROR_MARK_MISSING_ARGUMENT(X) X(PCRE2_ERROR_MARK_MISSING_ARGUMENT)
#else
#  define HANDLE_PCRE2_ERROR_MARK_MISSING_ARGUMENT(X)
#endif
#ifdef PCRE2_ERROR_INVALID_HEXADECIMAL
#  define HANDLE_PCRE2_ERROR_INVALID_HEXADECIMAL(X) X(PCRE2_ERROR_INVALID_HEXADECIMAL)
#else
#  define HANDLE_PCRE2_ERROR_INVALID_HEXADECIMAL(X)
#endif
#ifdef PCRE2_ERROR_BACKSLASH_C_SYNTAX
#  define HANDLE_PCRE2_ERROR_BACKSLASH_C_SYNTAX(X) X(PCRE2_ERROR_BACKSLASH_C_SYNTAX)
#else
#  define HANDLE_PCRE2_ERROR_BACKSLASH_C_SYNTAX(X)
#endif
#ifdef PCRE2_ERROR_BACKSLASH_K_SYNTAX
#  define HANDLE_PCRE2_ERROR_BACKSLASH_K_SYNTAX(X) X(PCRE2_ERROR_BACKSLASH_K_SYNTAX)
#else
#  define HANDLE_PCRE2_ERROR_BACKSLASH_K_SYNTAX(X)
#endif
#ifdef PCRE2_ERROR_INTERNAL_BAD_CODE_LOOKBEHINDS
#  define HANDLE_PCRE2_ERROR_INTERNAL_BAD_CODE_LOOKBEHINDS(X) X(PCRE2_ERROR_INTERNAL_BAD_CODE_LOOKBEHINDS)
#else
#  define HANDLE_PCRE2_ERROR_INTERNAL_BAD_CODE_LOOKBEHINDS(X)
#endif
#ifdef PCRE2_ERROR_BACKSLASH_N_IN_CLASS
#  define HANDLE_PCRE2_ERROR_BACKSLASH_N_IN_CLASS(X) X(PCRE2_ERROR_BACKSLASH_N_IN_CLASS)
#else
#  define HANDLE_PCRE2_ERROR_BACKSLASH_N_IN_CLASS(X)
#endif
#ifdef PCRE2_ERROR_CALLOUT_STRING_TOO_LONG
#  define HANDLE_PCRE2_ERROR_CALLOUT_STRING_TOO_LONG(X) X(PCRE2_ERROR_CALLOUT_STRING_TOO_LONG)
#else
#  define HANDLE_PCRE2_ERROR_CALLOUT_STRING_TOO_LONG(X)
#endif
#ifdef PCRE2_ERROR_UNICODE_DISALLOWED_CODE_POINT
#  define HANDLE_PCRE2_ERROR_UNICODE_DISALLOWED_CODE_POINT(X) X(PCRE2_ERROR_UNICODE_DISALLOWED_CODE_POINT)
#else
#  define HANDLE_PCRE2_ERROR_UNICODE_DISALLOWED_CODE_POINT(X)
#endif
#ifdef PCRE2_ERROR_UTF_IS_DISABLED
#  define HANDLE_PCRE2_ERROR_UTF_IS_DISABLED(X) X(PCRE2_ERROR_UTF_IS_DISABLED)
#else
#  define HANDLE_PCRE2_ERROR_UTF_IS_DISABLED(X)
#endif
#ifdef PCRE2_ERROR_UCP_IS_DISABLED
#  define HANDLE_PCRE2_ERROR_UCP_IS_DISABLED(X) X(PCRE2_ERROR_UCP_IS_DISABLED)
#else
#  define HANDLE_PCRE2_ERROR_UCP_IS_DISABLED(X)
#endif
#ifdef PCRE2_ERROR_VERB_NAME_TOO_LONG
#  define HANDLE_PCRE2_ERROR_VERB_NAME_TOO_LONG(X) X(PCRE2_ERROR_VERB_NAME_TOO_LONG)
#else
#  define HANDLE_PCRE2_ERROR_VERB_NAME_TOO_LONG(X)
#endif
#ifdef PCRE2_ERROR_BACKSLASH_U_CODE_POINT_TOO_BIG
#  define HANDLE_PCRE2_ERROR_BACKSLASH_U_CODE_POINT_TOO_BIG(X) X(PCRE2_ERROR_BACKSLASH_U_CODE_POINT_TOO_BIG)
#else
#  define HANDLE_PCRE2_ERROR_BACKSLASH_U_CODE_POINT_TOO_BIG(X)
#endif
#ifdef PCRE2_ERROR_MISSING_OCTAL_OR_HEX_DIGITS
#  define HANDLE_PCRE2_ERROR_MISSING_OCTAL_OR_HEX_DIGITS(X) X(PCRE2_ERROR_MISSING_OCTAL_OR_HEX_DIGITS)
#else
#  define HANDLE_PCRE2_ERROR_MISSING_OCTAL_OR_HEX_DIGITS(X)
#endif
#ifdef PCRE2_ERROR_VERSION_CONDITION_SYNTAX
#  define HANDLE_PCRE2_ERROR_VERSION_CONDITION_SYNTAX(X) X(PCRE2_ERROR_VERSION_CONDITION_SYNTAX)
#else
#  define HANDLE_PCRE2_ERROR_VERSION_CONDITION_SYNTAX(X)
#endif
#ifdef PCRE2_ERROR_INTERNAL_BAD_CODE_AUTO_POSSESS
#  define HANDLE_PCRE2_ERROR_INTERNAL_BAD_CODE_AUTO_POSSESS(X) X(PCRE2_ERROR_INTERNAL_BAD_CODE_AUTO_POSSESS)
#else
#  define HANDLE_PCRE2_ERROR_INTERNAL_BAD_CODE_AUTO_POSSESS(X)
#endif
#ifdef PCRE2_ERROR_CALLOUT_NO_STRING_DELIMITER
#  define HANDLE_PCRE2_ERROR_CALLOUT_NO_STRING_DELIMITER(X) X(PCRE2_ERROR_CALLOUT_NO_STRING_DELIMITER)
#else
#  define HANDLE_PCRE2_ERROR_CALLOUT_NO_STRING_DELIMITER(X)
#endif
#ifdef PCRE2_ERROR_CALLOUT_BAD_STRING_DELIMITER
#  define HANDLE_PCRE2_ERROR_CALLOUT_BAD_STRING_DELIMITER(X) X(PCRE2_ERROR_CALLOUT_BAD_STRING_DELIMITER)
#else
#  define HANDLE_PCRE2_ERROR_CALLOUT_BAD_STRING_DELIMITER(X)
#endif
#ifdef PCRE2_ERROR_BACKSLASH_C_CALLER_DISABLED
#  define HANDLE_PCRE2_ERROR_BACKSLASH_C_CALLER_DISABLED(X) X(PCRE2_ERROR_BACKSLASH_C_CALLER_DISABLED)
#else
#  define HANDLE_PCRE2_ERROR_BACKSLASH_C_CALLER_DISABLED(X)
#endif
#ifdef PCRE2_ERROR_QUERY_BARJX_NEST_TOO_DEEP
#  define HANDLE_PCRE2_ERROR_QUERY_BARJX_NEST_TOO_DEEP(X) X(PCRE2_ERROR_QUERY_BARJX_NEST_TOO_DEEP)
#else
#  define HANDLE_PCRE2_ERROR_QUERY_BARJX_NEST_TOO_DEEP(X)
#endif
#ifdef PCRE2_ERROR_BACKSLASH_C_LIBRARY_DISABLED
#  define HANDLE_PCRE2_ERROR_BACKSLASH_C_LIBRARY_DISABLED(X) X(PCRE2_ERROR_BACKSLASH_C_LIBRARY_DISABLED)
#else
#  define HANDLE_PCRE2_ERROR_BACKSLASH_C_LIBRARY_DISABLED(X)
#endif
#ifdef PCRE2_ERROR_PATTERN_TOO_COMPLICATED
#  define HANDLE_PCRE2_ERROR_PATTERN_TOO_COMPLICATED(X) X(PCRE2_ERROR_PATTERN_TOO_COMPLICATED)
#else
#  define HANDLE_PCRE2_ERROR_PATTERN_TOO_COMPLICATED(X)
#endif
#ifdef PCRE2_ERROR_LOOKBEHIND_TOO_LONG
#  define HANDLE_PCRE2_ERROR_LOOKBEHIND_TOO_LONG(X) X(PCRE2_ERROR_LOOKBEHIND_TOO_LONG)
#else
#  define HANDLE_PCRE2_ERROR_LOOKBEHIND_TOO_LONG(X)
#endif
#ifdef PCRE2_ERROR_PATTERN_STRING_TOO_LONG
#  define HANDLE_PCRE2_ERROR_PATTERN_STRING_TOO_LONG(X) X(PCRE2_ERROR_PATTERN_STRING_TOO_LONG)
#else
#  define HANDLE_PCRE2_ERROR_PATTERN_STRING_TOO_LONG(X)
#endif
#ifdef PCRE2_ERROR_INTERNAL_BAD_CODE
#  define HANDLE_PCRE2_ERROR_INTERNAL_BAD_CODE(X) X(PCRE2_ERROR_INTERNAL_BAD_CODE)
#else
#  define HANDLE_PCRE2_ERROR_INTERNAL_BAD_CODE(X)
#endif
#ifdef PCRE2_ERROR_INTERNAL_BAD_CODE_IN_SKIP
#  define HANDLE_PCRE2_ERROR_INTERNAL_BAD_CODE_IN_SKIP(X) X(PCRE2_ERROR_INTERNAL_BAD_CODE_IN_SKIP)
#else
#  define HANDLE_PCRE2_ERROR_INTERNAL_BAD_CODE_IN_SKIP(X)
#endif
#ifdef PCRE2_ERROR_NO_SURROGATES_IN_UTF16
#  define HANDLE_PCRE2_ERROR_NO_SURROGATES_IN_UTF16(X) X(PCRE2_ERROR_NO_SURROGATES_IN_UTF16)
#else
#  define HANDLE_PCRE2_ERROR_NO_SURROGATES_IN_UTF16(X)
#endif
#ifdef PCRE2_ERROR_BAD_LITERAL_OPTIONS
#  define HANDLE_PCRE2_ERROR_BAD_LITERAL_OPTIONS(X) X(PCRE2_ERROR_BAD_LITERAL_OPTIONS)
#else
#  define HANDLE_PCRE2_ERROR_BAD_LITERAL_OPTIONS(X)
#endif
#ifdef PCRE2_ERROR_SUPPORTED_ONLY_IN_UNICODE
#  define HANDLE_PCRE2_ERROR_SUPPORTED_ONLY_IN_UNICODE(X) X(PCRE2_ERROR_SUPPORTED_ONLY_IN_UNICODE)
#else
#  define HANDLE_PCRE2_ERROR_SUPPORTED_ONLY_IN_UNICODE(X)
#endif
#ifdef PCRE2_ERROR_INVALID_HYPHEN_IN_OPTIONS
#  define HANDLE_PCRE2_ERROR_INVALID_HYPHEN_IN_OPTIONS(X) X(PCRE2_ERROR_INVALID_HYPHEN_IN_OPTIONS)
#else
#  define HANDLE_PCRE2_ERROR_INVALID_HYPHEN_IN_OPTIONS(X)
#endif
#ifdef PCRE2_ERROR_ALPHA_ASSERTION_UNKNOWN
#  define HANDLE_PCRE2_ERROR_ALPHA_ASSERTION_UNKNOWN(X) X(PCRE2_ERROR_ALPHA_ASSERTION_UNKNOWN)
#else
#  define HANDLE_PCRE2_ERROR_ALPHA_ASSERTION_UNKNOWN(X)
#endif
#ifdef PCRE2_ERROR_SCRIPT_RUN_NOT_AVAILABLE
#  define HANDLE_PCRE2_ERROR_SCRIPT_RUN_NOT_AVAILABLE(X) X(PCRE2_ERROR_SCRIPT_RUN_NOT_AVAILABLE)
#else
#  define HANDLE_PCRE2_ERROR_SCRIPT_RUN_NOT_AVAILABLE(X)
#endif
#ifdef PCRE2_ERROR_TOO_MANY_CAPTURES
#  define HANDLE_PCRE2_ERROR_TOO_MANY_CAPTURES(X) X(PCRE2_ERROR_TOO_MANY_CAPTURES)
#else
#  define HANDLE_PCRE2_ERROR_TOO_MANY_CAPTURES(X)
#endif
#ifdef PCRE2_ERROR_MISSING_OCTAL_DIGIT
#  define HANDLE_PCRE2_ERROR_MISSING_OCTAL_DIGIT(X) X(PCRE2_ERROR_MISSING_OCTAL_DIGIT)
#else
#  define HANDLE_PCRE2_ERROR_MISSING_OCTAL_DIGIT(X)
#endif
#ifdef PCRE2_ERROR_BACKSLASH_K_IN_LOOKAROUND
#  define HANDLE_PCRE2_ERROR_BACKSLASH_K_IN_LOOKAROUND(X) X(PCRE2_ERROR_BACKSLASH_K_IN_LOOKAROUND)
#else
#  define HANDLE_PCRE2_ERROR_BACKSLASH_K_IN_LOOKAROUND(X)
#endif
#ifdef PCRE2_ERROR_MAX_VAR_LOOKBEHIND_EXCEEDED
#  define HANDLE_PCRE2_ERROR_MAX_VAR_LOOKBEHIND_EXCEEDED(X) X(PCRE2_ERROR_MAX_VAR_LOOKBEHIND_EXCEEDED)
#else
#  define HANDLE_PCRE2_ERROR_MAX_VAR_LOOKBEHIND_EXCEEDED(X)
#endif
#ifdef PCRE2_ERROR_PATTERN_COMPILED_SIZE_TOO_BIG
#  define HANDLE_PCRE2_ERROR_PATTERN_COMPILED_SIZE_TOO_BIG(X) X(PCRE2_ERROR_PATTERN_COMPILED_SIZE_TOO_BIG)
#else
#  define HANDLE_PCRE2_ERROR_PATTERN_COMPILED_SIZE_TOO_BIG(X)
#endif
#ifdef PCRE2_ERROR_OVERSIZE_PYTHON_OCTAL
#  define HANDLE_PCRE2_ERROR_OVERSIZE_PYTHON_OCTAL(X) X(PCRE2_ERROR_OVERSIZE_PYTHON_OCTAL)
#else
#  define HANDLE_PCRE2_ERROR_OVERSIZE_PYTHON_OCTAL(X)
#endif
#ifdef PCRE2_ERROR_CALLOUT_CALLER_DISABLED
#  define HANDLE_PCRE2_ERROR_CALLOUT_CALLER_DISABLED(X) X(PCRE2_ERROR_CALLOUT_CALLER_DISABLED)
#else
#  define HANDLE_PCRE2_ERROR_CALLOUT_CALLER_DISABLED(X)
#endif
#ifdef PCRE2_ERROR_EXTRA_CASING_REQUIRES_UNICODE
#  define HANDLE_PCRE2_ERROR_EXTRA_CASING_REQUIRES_UNICODE(X) X(PCRE2_ERROR_EXTRA_CASING_REQUIRES_UNICODE)
#else
#  define HANDLE_PCRE2_ERROR_EXTRA_CASING_REQUIRES_UNICODE(X)
#endif
#ifdef PCRE2_ERROR_TURKISH_CASING_REQUIRES_UTF
#  define HANDLE_PCRE2_ERROR_TURKISH_CASING_REQUIRES_UTF(X) X(PCRE2_ERROR_TURKISH_CASING_REQUIRES_UTF)
#else
#  define HANDLE_PCRE2_ERROR_TURKISH_CASING_REQUIRES_UTF(X)
#endif
#ifdef PCRE2_ERROR_EXTRA_CASING_INCOMPATIBLE
#  define HANDLE_PCRE2_ERROR_EXTRA_CASING_INCOMPATIBLE(X) X(PCRE2_ERROR_EXTRA_CASING_INCOMPATIBLE)
#else
#  define HANDLE_PCRE2_ERROR_EXTRA_CASING_INCOMPATIBLE(X)
#endif
#ifdef PCRE2_ERROR_ECLASS_NEST_TOO_DEEP
#  define HANDLE_PCRE2_ERROR_ECLASS_NEST_TOO_DEEP(X) X(PCRE2_ERROR_ECLASS_NEST_TOO_DEEP)
#else
#  define HANDLE_PCRE2_ERROR_ECLASS_NEST_TOO_DEEP(X)
#endif
#ifdef PCRE2_ERROR_ECLASS_INVALID_OPERATOR
#  define HANDLE_PCRE2_ERROR_ECLASS_INVALID_OPERATOR(X) X(PCRE2_ERROR_ECLASS_INVALID_OPERATOR)
#else
#  define HANDLE_PCRE2_ERROR_ECLASS_INVALID_OPERATOR(X)
#endif
#ifdef PCRE2_ERROR_ECLASS_UNEXPECTED_OPERATOR
#  define HANDLE_PCRE2_ERROR_ECLASS_UNEXPECTED_OPERATOR(X) X(PCRE2_ERROR_ECLASS_UNEXPECTED_OPERATOR)
#else
#  define HANDLE_PCRE2_ERROR_ECLASS_UNEXPECTED_OPERATOR(X)
#endif
#ifdef PCRE2_ERROR_ECLASS_EXPECTED_OPERAND
#  define HANDLE_PCRE2_ERROR_ECLASS_EXPECTED_OPERAND(X) X(PCRE2_ERROR_ECLASS_EXPECTED_OPERAND)
#else
#  define HANDLE_PCRE2_ERROR_ECLASS_EXPECTED_OPERAND(X)
#endif
#ifdef PCRE2_ERROR_ECLASS_MIXED_OPERATORS
#  define HANDLE_PCRE2_ERROR_ECLASS_MIXED_OPERATORS(X) X(PCRE2_ERROR_ECLASS_MIXED_OPERATORS)
#else
#  define HANDLE_PCRE2_ERROR_ECLASS_MIXED_OPERATORS(X)
#endif
#ifdef PCRE2_ERROR_ECLASS_HINT_SQUARE_BRACKET
#  define HANDLE_PCRE2_ERROR_ECLASS_HINT_SQUARE_BRACKET(X) X(PCRE2_ERROR_ECLASS_HINT_SQUARE_BRACKET)
#else
#  define HANDLE_PCRE2_ERROR_ECLASS_HINT_SQUARE_BRACKET(X)
#endif
#ifdef PCRE2_ERROR_PERL_ECLASS_UNEXPECTED_EXPR
#  define HANDLE_PCRE2_ERROR_PERL_ECLASS_UNEXPECTED_EXPR(X) X(PCRE2_ERROR_PERL_ECLASS_UNEXPECTED_EXPR)
#else
#  define HANDLE_PCRE2_ERROR_PERL_ECLASS_UNEXPECTED_EXPR(X)
#endif
#ifdef PCRE2_ERROR_PERL_ECLASS_EMPTY_EXPR
#  define HANDLE_PCRE2_ERROR_PERL_ECLASS_EMPTY_EXPR(X) X(PCRE2_ERROR_PERL_ECLASS_EMPTY_EXPR)
#else
#  define HANDLE_PCRE2_ERROR_PERL_ECLASS_EMPTY_EXPR(X)
#endif
#ifdef PCRE2_ERROR_PERL_ECLASS_MISSING_CLOSE
#  define HANDLE_PCRE2_ERROR_PERL_ECLASS_MISSING_CLOSE(X) X(PCRE2_ERROR_PERL_ECLASS_MISSING_CLOSE)
#else
#  define HANDLE_PCRE2_ERROR_PERL_ECLASS_MISSING_CLOSE(X)
#endif
#ifdef PCRE2_ERROR_PERL_ECLASS_UNEXPECTED_CHAR
#  define HANDLE_PCRE2_ERROR_PERL_ECLASS_UNEXPECTED_CHAR(X) X(PCRE2_ERROR_PERL_ECLASS_UNEXPECTED_CHAR)
#else
#  define HANDLE_PCRE2_ERROR_PERL_ECLASS_UNEXPECTED_CHAR(X)
#endif
#ifdef PCRE2_ERROR_EXPECTED_CAPTURE_GROUP
#  define HANDLE_PCRE2_ERROR_EXPECTED_CAPTURE_GROUP(X) X(PCRE2_ERROR_EXPECTED_CAPTURE_GROUP)
#else
#  define HANDLE_PCRE2_ERROR_EXPECTED_CAPTURE_GROUP(X)
#endif
#ifdef PCRE2_ERROR_MISSING_OPENING_PARENTHESIS
#  define HANDLE_PCRE2_ERROR_MISSING_OPENING_PARENTHESIS(X) X(PCRE2_ERROR_MISSING_OPENING_PARENTHESIS)
#else
#  define HANDLE_PCRE2_ERROR_MISSING_OPENING_PARENTHESIS(X)
#endif
#ifdef PCRE2_ERROR_MISSING_NUMBER_TERMINATOR
#  define HANDLE_PCRE2_ERROR_MISSING_NUMBER_TERMINATOR(X) X(PCRE2_ERROR_MISSING_NUMBER_TERMINATOR)
#else
#  define HANDLE_PCRE2_ERROR_MISSING_NUMBER_TERMINATOR(X)
#endif
#ifdef PCRE2_ERROR_NULL_ERROROFFSET
#  define HANDLE_PCRE2_ERROR_NULL_ERROROFFSET(X) X(PCRE2_ERROR_NULL_ERROROFFSET)
#else
#  define HANDLE_PCRE2_ERROR_NULL_ERROROFFSET(X)
#endif
#ifdef PCRE2_ERROR_NOMATCH
#  define HANDLE_PCRE2_ERROR_NOMATCH(X) X(PCRE2_ERROR_NOMATCH)
#else
#  define HANDLE_PCRE2_ERROR_NOMATCH(X)
#endif
#ifdef PCRE2_ERROR_PARTIAL
#  define HANDLE_PCRE2_ERROR_PARTIAL(X) X(PCRE2_ERROR_PARTIAL)
#else
#  define HANDLE_PCRE2_ERROR_PARTIAL(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR1
#  define HANDLE_PCRE2_ERROR_UTF8_ERR1(X) X(PCRE2_ERROR_UTF8_ERR1)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR1(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR2
#  define HANDLE_PCRE2_ERROR_UTF8_ERR2(X) X(PCRE2_ERROR_UTF8_ERR2)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR2(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR3
#  define HANDLE_PCRE2_ERROR_UTF8_ERR3(X) X(PCRE2_ERROR_UTF8_ERR3)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR3(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR4
#  define HANDLE_PCRE2_ERROR_UTF8_ERR4(X) X(PCRE2_ERROR_UTF8_ERR4)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR4(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR5
#  define HANDLE_PCRE2_ERROR_UTF8_ERR5(X) X(PCRE2_ERROR_UTF8_ERR5)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR5(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR6
#  define HANDLE_PCRE2_ERROR_UTF8_ERR6(X) X(PCRE2_ERROR_UTF8_ERR6)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR6(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR7
#  define HANDLE_PCRE2_ERROR_UTF8_ERR7(X) X(PCRE2_ERROR_UTF8_ERR7)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR7(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR8
#  define HANDLE_PCRE2_ERROR_UTF8_ERR8(X) X(PCRE2_ERROR_UTF8_ERR8)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR8(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR9
#  define HANDLE_PCRE2_ERROR_UTF8_ERR9(X) X(PCRE2_ERROR_UTF8_ERR9)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR9(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR10
#  define HANDLE_PCRE2_ERROR_UTF8_ERR10(X) X(PCRE2_ERROR_UTF8_ERR10)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR10(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR11
#  define HANDLE_PCRE2_ERROR_UTF8_ERR11(X) X(PCRE2_ERROR_UTF8_ERR11)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR11(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR12
#  define HANDLE_PCRE2_ERROR_UTF8_ERR12(X) X(PCRE2_ERROR_UTF8_ERR12)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR12(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR13
#  define HANDLE_PCRE2_ERROR_UTF8_ERR13(X) X(PCRE2_ERROR_UTF8_ERR13)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR13(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR14
#  define HANDLE_PCRE2_ERROR_UTF8_ERR14(X) X(PCRE2_ERROR_UTF8_ERR14)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR14(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR15
#  define HANDLE_PCRE2_ERROR_UTF8_ERR15(X) X(PCRE2_ERROR_UTF8_ERR15)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR15(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR16
#  define HANDLE_PCRE2_ERROR_UTF8_ERR16(X) X(PCRE2_ERROR_UTF8_ERR16)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR16(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR17
#  define HANDLE_PCRE2_ERROR_UTF8_ERR17(X) X(PCRE2_ERROR_UTF8_ERR17)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR17(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR18
#  define HANDLE_PCRE2_ERROR_UTF8_ERR18(X) X(PCRE2_ERROR_UTF8_ERR18)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR18(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR19
#  define HANDLE_PCRE2_ERROR_UTF8_ERR19(X) X(PCRE2_ERROR_UTF8_ERR19)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR19(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR20
#  define HANDLE_PCRE2_ERROR_UTF8_ERR20(X) X(PCRE2_ERROR_UTF8_ERR20)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR20(X)
#endif
#ifdef PCRE2_ERROR_UTF8_ERR21
#  define HANDLE_PCRE2_ERROR_UTF8_ERR21(X) X(PCRE2_ERROR_UTF8_ERR21)
#else
#  define HANDLE_PCRE2_ERROR_UTF8_ERR21(X)
#endif
#ifdef PCRE2_ERROR_UTF16_ERR1
#  define HANDLE_PCRE2_ERROR_UTF16_ERR1(X) X(PCRE2_ERROR_UTF16_ERR1)
#else
#  define HANDLE_PCRE2_ERROR_UTF16_ERR1(X)
#endif
#ifdef PCRE2_ERROR_UTF16_ERR2
#  define HANDLE_PCRE2_ERROR_UTF16_ERR2(X) X(PCRE2_ERROR_UTF16_ERR2)
#else
#  define HANDLE_PCRE2_ERROR_UTF16_ERR2(X)
#endif
#ifdef PCRE2_ERROR_UTF16_ERR3
#  define HANDLE_PCRE2_ERROR_UTF16_ERR3(X) X(PCRE2_ERROR_UTF16_ERR3)
#else
#  define HANDLE_PCRE2_ERROR_UTF16_ERR3(X)
#endif
#ifdef PCRE2_ERROR_UTF32_ERR1
#  define HANDLE_PCRE2_ERROR_UTF32_ERR1(X) X(PCRE2_ERROR_UTF32_ERR1)
#else
#  define HANDLE_PCRE2_ERROR_UTF32_ERR1(X)
#endif
#ifdef PCRE2_ERROR_UTF32_ERR2
#  define HANDLE_PCRE2_ERROR_UTF32_ERR2(X) X(PCRE2_ERROR_UTF32_ERR2)
#else
#  define HANDLE_PCRE2_ERROR_UTF32_ERR2(X)
#endif
#ifdef PCRE2_ERROR_BADDATA
#  define HANDLE_PCRE2_ERROR_BADDATA(X) X(PCRE2_ERROR_BADDATA)
#else
#  define HANDLE_PCRE2_ERROR_BADDATA(X)
#endif
#ifdef PCRE2_ERROR_MIXEDTABLES
#  define HANDLE_PCRE2_ERROR_MIXEDTABLES(X) X(PCRE2_ERROR_MIXEDTABLES)
#else
#  define HANDLE_PCRE2_ERROR_MIXEDTABLES(X)
#endif
#ifdef PCRE2_ERROR_BADMAGIC
#  define HANDLE_PCRE2_ERROR_BADMAGIC(X) X(PCRE2_ERROR_BADMAGIC)
#else
#  define HANDLE_PCRE2_ERROR_BADMAGIC(X)
#endif
#ifdef PCRE2_ERROR_BADMODE
#  define HANDLE_PCRE2_ERROR_BADMODE(X) X(PCRE2_ERROR_BADMODE)
#else
#  define HANDLE_PCRE2_ERROR_BADMODE(X)
#endif
#ifdef PCRE2_ERROR_BADOFFSET
#  define HANDLE_PCRE2_ERROR_BADOFFSET(X) X(PCRE2_ERROR_BADOFFSET)
#else
#  define HANDLE_PCRE2_ERROR_BADOFFSET(X)
#endif
#ifdef PCRE2_ERROR_BADOPTION
#  define HANDLE_PCRE2_ERROR_BADOPTION(X) X(PCRE2_ERROR_BADOPTION)
#else
#  define HANDLE_PCRE2_ERROR_BADOPTION(X)
#endif
#ifdef PCRE2_ERROR_BADREPLACEMENT
#  define HANDLE_PCRE2_ERROR_BADREPLACEMENT(X) X(PCRE2_ERROR_BADREPLACEMENT)
#else
#  define HANDLE_PCRE2_ERROR_BADREPLACEMENT(X)
#endif
#ifdef PCRE2_ERROR_BADUTFOFFSET
#  define HANDLE_PCRE2_ERROR_BADUTFOFFSET(X) X(PCRE2_ERROR_BADUTFOFFSET)
#else
#  define HANDLE_PCRE2_ERROR_BADUTFOFFSET(X)
#endif
#ifdef PCRE2_ERROR_CALLOUT
#  define HANDLE_PCRE2_ERROR_CALLOUT(X) X(PCRE2_ERROR_CALLOUT)
#else
#  define HANDLE_PCRE2_ERROR_CALLOUT(X)
#endif
#ifdef PCRE2_ERROR_DFA_BADRESTART
#  define HANDLE_PCRE2_ERROR_DFA_BADRESTART(X) X(PCRE2_ERROR_DFA_BADRESTART)
#else
#  define HANDLE_PCRE2_ERROR_DFA_BADRESTART(X)
#endif
#ifdef PCRE2_ERROR_DFA_RECURSE
#  define HANDLE_PCRE2_ERROR_DFA_RECURSE(X) X(PCRE2_ERROR_DFA_RECURSE)
#else
#  define HANDLE_PCRE2_ERROR_DFA_RECURSE(X)
#endif
#ifdef PCRE2_ERROR_DFA_UCOND
#  define HANDLE_PCRE2_ERROR_DFA_UCOND(X) X(PCRE2_ERROR_DFA_UCOND)
#else
#  define HANDLE_PCRE2_ERROR_DFA_UCOND(X)
#endif
#ifdef PCRE2_ERROR_DFA_UFUNC
#  define HANDLE_PCRE2_ERROR_DFA_UFUNC(X) X(PCRE2_ERROR_DFA_UFUNC)
#else
#  define HANDLE_PCRE2_ERROR_DFA_UFUNC(X)
#endif
#ifdef PCRE2_ERROR_DFA_UITEM
#  define HANDLE_PCRE2_ERROR_DFA_UITEM(X) X(PCRE2_ERROR_DFA_UITEM)
#else
#  define HANDLE_PCRE2_ERROR_DFA_UITEM(X)
#endif
#ifdef PCRE2_ERROR_DFA_WSSIZE
#  define HANDLE_PCRE2_ERROR_DFA_WSSIZE(X) X(PCRE2_ERROR_DFA_WSSIZE)
#else
#  define HANDLE_PCRE2_ERROR_DFA_WSSIZE(X)
#endif
#ifdef PCRE2_ERROR_INTERNAL
#  define HANDLE_PCRE2_ERROR_INTERNAL(X) X(PCRE2_ERROR_INTERNAL)
#else
#  define HANDLE_PCRE2_ERROR_INTERNAL(X)
#endif
#ifdef PCRE2_ERROR_JIT_BADOPTION
#  define HANDLE_PCRE2_ERROR_JIT_BADOPTION(X) X(PCRE2_ERROR_JIT_BADOPTION)
#else
#  define HANDLE_PCRE2_ERROR_JIT_BADOPTION(X)
#endif
#ifdef PCRE2_ERROR_JIT_STACKLIMIT
#  define HANDLE_PCRE2_ERROR_JIT_STACKLIMIT(X) X(PCRE2_ERROR_JIT_STACKLIMIT)
#else
#  define HANDLE_PCRE2_ERROR_JIT_STACKLIMIT(X)
#endif
#ifdef PCRE2_ERROR_MATCHLIMIT
#  define HANDLE_PCRE2_ERROR_MATCHLIMIT(X) X(PCRE2_ERROR_MATCHLIMIT)
#else
#  define HANDLE_PCRE2_ERROR_MATCHLIMIT(X)
#endif
#ifdef PCRE2_ERROR_NOMEMORY
#  define HANDLE_PCRE2_ERROR_NOMEMORY(X) X(PCRE2_ERROR_NOMEMORY)
#else
#  define HANDLE_PCRE2_ERROR_NOMEMORY(X)
#endif
#ifdef PCRE2_ERROR_NOSUBSTRING
#  define HANDLE_PCRE2_ERROR_NOSUBSTRING(X) X(PCRE2_ERROR_NOSUBSTRING)
#else
#  define HANDLE_PCRE2_ERROR_NOSUBSTRING(X)
#endif
#ifdef PCRE2_ERROR_NOUNIQUESUBSTRING
#  define HANDLE_PCRE2_ERROR_NOUNIQUESUBSTRING(X) X(PCRE2_ERROR_NOUNIQUESUBSTRING)
#else
#  define HANDLE_PCRE2_ERROR_NOUNIQUESUBSTRING(X)
#endif
#ifdef PCRE2_ERROR_NULL
#  define HANDLE_PCRE2_ERROR_NULL(X) X(PCRE2_ERROR_NULL)
#else
#  define HANDLE_PCRE2_ERROR_NULL(X)
#endif
#ifdef PCRE2_ERROR_RECURSELOOP
#  define HANDLE_PCRE2_ERROR_RECURSELOOP(X) X(PCRE2_ERROR_RECURSELOOP)
#else
#  define HANDLE_PCRE2_ERROR_RECURSELOOP(X)
#endif
#ifdef PCRE2_ERROR_DEPTHLIMIT
#  define HANDLE_PCRE2_ERROR_DEPTHLIMIT(X) X(PCRE2_ERROR_DEPTHLIMIT)
#else
#  define HANDLE_PCRE2_ERROR_DEPTHLIMIT(X)
#endif
#ifdef PCRE2_ERROR_RECURSIONLIMIT
#  define HANDLE_PCRE2_ERROR_RECURSIONLIMIT(X) X(PCRE2_ERROR_RECURSIONLIMIT)
#else
#  define HANDLE_PCRE2_ERROR_RECURSIONLIMIT(X)
#endif
#ifdef PCRE2_ERROR_UNAVAILABLE
#  define HANDLE_PCRE2_ERROR_UNAVAILABLE(X) X(PCRE2_ERROR_UNAVAILABLE)
#else
#  define HANDLE_PCRE2_ERROR_UNAVAILABLE(X)
#endif
#ifdef PCRE2_ERROR_UNSET
#  define HANDLE_PCRE2_ERROR_UNSET(X) X(PCRE2_ERROR_UNSET)
#else
#  define HANDLE_PCRE2_ERROR_UNSET(X)
#endif
#ifdef PCRE2_ERROR_BADOFFSETLIMIT
#  define HANDLE_PCRE2_ERROR_BADOFFSETLIMIT(X) X(PCRE2_ERROR_BADOFFSETLIMIT)
#else
#  define HANDLE_PCRE2_ERROR_BADOFFSETLIMIT(X)
#endif
#ifdef PCRE2_ERROR_BADREPESCAPE
#  define HANDLE_PCRE2_ERROR_BADREPESCAPE(X) X(PCRE2_ERROR_BADREPESCAPE)
#else
#  define HANDLE_PCRE2_ERROR_BADREPESCAPE(X)
#endif
#ifdef PCRE2_ERROR_REPMISSINGBRACE
#  define HANDLE_PCRE2_ERROR_REPMISSINGBRACE(X) X(PCRE2_ERROR_REPMISSINGBRACE)
#else
#  define HANDLE_PCRE2_ERROR_REPMISSINGBRACE(X)
#endif
#ifdef PCRE2_ERROR_BADSUBSTITUTION
#  define HANDLE_PCRE2_ERROR_BADSUBSTITUTION(X) X(PCRE2_ERROR_BADSUBSTITUTION)
#else
#  define HANDLE_PCRE2_ERROR_BADSUBSTITUTION(X)
#endif
#ifdef PCRE2_ERROR_BADSUBSPATTERN
#  define HANDLE_PCRE2_ERROR_BADSUBSPATTERN(X) X(PCRE2_ERROR_BADSUBSPATTERN)
#else
#  define HANDLE_PCRE2_ERROR_BADSUBSPATTERN(X)
#endif
#ifdef PCRE2_ERROR_TOOMANYREPLACE
#  define HANDLE_PCRE2_ERROR_TOOMANYREPLACE(X) X(PCRE2_ERROR_TOOMANYREPLACE)
#else
#  define HANDLE_PCRE2_ERROR_TOOMANYREPLACE(X)
#endif
#ifdef PCRE2_ERROR_BADSERIALIZEDDATA
#  define HANDLE_PCRE2_ERROR_BADSERIALIZEDDATA(X) X(PCRE2_ERROR_BADSERIALIZEDDATA)
#else
#  define HANDLE_PCRE2_ERROR_BADSERIALIZEDDATA(X)
#endif
#ifdef PCRE2_ERROR_HEAPLIMIT
#  define HANDLE_PCRE2_ERROR_HEAPLIMIT(X) X(PCRE2_ERROR_HEAPLIMIT)
#else
#  define HANDLE_PCRE2_ERROR_HEAPLIMIT(X)
#endif
#ifdef PCRE2_ERROR_CONVERT_SYNTAX
#  define HANDLE_PCRE2_ERROR_CONVERT_SYNTAX(X) X(PCRE2_ERROR_CONVERT_SYNTAX)
#else
#  define HANDLE_PCRE2_ERROR_CONVERT_SYNTAX(X)
#endif
#ifdef PCRE2_ERROR_INTERNAL_DUPMATCH
#  define HANDLE_PCRE2_ERROR_INTERNAL_DUPMATCH(X) X(PCRE2_ERROR_INTERNAL_DUPMATCH)
#else
#  define HANDLE_PCRE2_ERROR_INTERNAL_DUPMATCH(X)
#endif
#ifdef PCRE2_ERROR_DFA_UINVALID_UTF
#  define HANDLE_PCRE2_ERROR_DFA_UINVALID_UTF(X) X(PCRE2_ERROR_DFA_UINVALID_UTF)
#else
#  define HANDLE_PCRE2_ERROR_DFA_UINVALID_UTF(X)
#endif
#ifdef PCRE2_ERROR_INVALIDOFFSET
#  define HANDLE_PCRE2_ERROR_INVALIDOFFSET(X) X(PCRE2_ERROR_INVALIDOFFSET)
#else
#  define HANDLE_PCRE2_ERROR_INVALIDOFFSET(X)
#endif
#ifdef PCRE2_ERROR_JIT_UNSUPPORTED
#  define HANDLE_PCRE2_ERROR_JIT_UNSUPPORTED(X) X(PCRE2_ERROR_JIT_UNSUPPORTED)
#else
#  define HANDLE_PCRE2_ERROR_JIT_UNSUPPORTED(X)
#endif
#ifdef PCRE2_ERROR_REPLACECASE
#  define HANDLE_PCRE2_ERROR_REPLACECASE(X) X(PCRE2_ERROR_REPLACECASE)
#else
#  define HANDLE_PCRE2_ERROR_REPLACECASE(X)
#endif
#ifdef PCRE2_ERROR_TOOLARGEREPLACE
#  define HANDLE_PCRE2_ERROR_TOOLARGEREPLACE(X) X(PCRE2_ERROR_TOOLARGEREPLACE)
#else
#  define HANDLE_PCRE2_ERROR_TOOLARGEREPLACE(X)
#endif
#ifdef PCRE2_ERROR_DIFFSUBSPATTERN
#  define HANDLE_PCRE2_ERROR_DIFFSUBSPATTERN(X) X(PCRE2_ERROR_DIFFSUBSPATTERN)
#else
#  define HANDLE_PCRE2_ERROR_DIFFSUBSPATTERN(X)
#endif
#ifdef PCRE2_ERROR_DIFFSUBSSUBJECT
#  define HANDLE_PCRE2_ERROR_DIFFSUBSSUBJECT(X) X(PCRE2_ERROR_DIFFSUBSSUBJECT)
#else
#  define HANDLE_PCRE2_ERROR_DIFFSUBSSUBJECT(X)
#endif
#ifdef PCRE2_ERROR_DIFFSUBSOFFSET
#  define HANDLE_PCRE2_ERROR_DIFFSUBSOFFSET(X) X(PCRE2_ERROR_DIFFSUBSOFFSET)
#else
#  define HANDLE_PCRE2_ERROR_DIFFSUBSOFFSET(X)
#endif
#ifdef PCRE2_ERROR_DIFFSUBSOPTIONS
#  define HANDLE_PCRE2_ERROR_DIFFSUBSOPTIONS(X) X(PCRE2_ERROR_DIFFSUBSOPTIONS)
#else
#  define HANDLE_PCRE2_ERROR_DIFFSUBSOPTIONS(X)
#endif

#define PCRE2_ERROR_LIST(X) \
    HANDLE_PCRE2_ERROR_END_BACKSLASH(X) \
    HANDLE_PCRE2_ERROR_END_BACKSLASH_C(X) \
    HANDLE_PCRE2_ERROR_UNKNOWN_ESCAPE(X) \
    HANDLE_PCRE2_ERROR_QUANTIFIER_OUT_OF_ORDER(X) \
    HANDLE_PCRE2_ERROR_QUANTIFIER_TOO_BIG(X) \
    HANDLE_PCRE2_ERROR_MISSING_SQUARE_BRACKET(X) \
    HANDLE_PCRE2_ERROR_ESCAPE_INVALID_IN_CLASS(X) \
    HANDLE_PCRE2_ERROR_CLASS_RANGE_ORDER(X) \
    HANDLE_PCRE2_ERROR_QUANTIFIER_INVALID(X) \
    HANDLE_PCRE2_ERROR_INTERNAL_UNEXPECTED_REPEAT(X) \
    HANDLE_PCRE2_ERROR_INVALID_AFTER_PARENS_QUERY(X) \
    HANDLE_PCRE2_ERROR_POSIX_CLASS_NOT_IN_CLASS(X) \
    HANDLE_PCRE2_ERROR_POSIX_NO_SUPPORT_COLLATING(X) \
    HANDLE_PCRE2_ERROR_MISSING_CLOSING_PARENTHESIS(X) \
    HANDLE_PCRE2_ERROR_BAD_SUBPATTERN_REFERENCE(X) \
    HANDLE_PCRE2_ERROR_NULL_PATTERN(X) \
    HANDLE_PCRE2_ERROR_BAD_OPTIONS(X) \
    HANDLE_PCRE2_ERROR_MISSING_COMMENT_CLOSING(X) \
    HANDLE_PCRE2_ERROR_PARENTHESES_NEST_TOO_DEEP(X) \
    HANDLE_PCRE2_ERROR_PATTERN_TOO_LARGE(X) \
    HANDLE_PCRE2_ERROR_HEAP_FAILED(X) \
    HANDLE_PCRE2_ERROR_UNMATCHED_CLOSING_PARENTHESIS(X) \
    HANDLE_PCRE2_ERROR_INTERNAL_CODE_OVERFLOW(X) \
    HANDLE_PCRE2_ERROR_MISSING_CONDITION_CLOSING(X) \
    HANDLE_PCRE2_ERROR_LOOKBEHIND_NOT_FIXED_LENGTH(X) \
    HANDLE_PCRE2_ERROR_ZERO_RELATIVE_REFERENCE(X) \
    HANDLE_PCRE2_ERROR_TOO_MANY_CONDITION_BRANCHES(X) \
    HANDLE_PCRE2_ERROR_CONDITION_ASSERTION_EXPECTED(X) \
    HANDLE_PCRE2_ERROR_BAD_RELATIVE_REFERENCE(X) \
    HANDLE_PCRE2_ERROR_UNKNOWN_POSIX_CLASS(X) \
    HANDLE_PCRE2_ERROR_INTERNAL_STUDY_ERROR(X) \
    HANDLE_PCRE2_ERROR_UNICODE_NOT_SUPPORTED(X) \
    HANDLE_PCRE2_ERROR_PARENTHESES_STACK_CHECK(X) \
    HANDLE_PCRE2_ERROR_CODE_POINT_TOO_BIG(X) \
    HANDLE_PCRE2_ERROR_LOOKBEHIND_TOO_COMPLICATED(X) \
    HANDLE_PCRE2_ERROR_LOOKBEHIND_INVALID_BACKSLASH_C(X) \
    HANDLE_PCRE2_ERROR_UNSUPPORTED_ESCAPE_SEQUENCE(X) \
    HANDLE_PCRE2_ERROR_CALLOUT_NUMBER_TOO_BIG(X) \
    HANDLE_PCRE2_ERROR_MISSING_CALLOUT_CLOSING(X) \
    HANDLE_PCRE2_ERROR_ESCAPE_INVALID_IN_VERB(X) \
    HANDLE_PCRE2_ERROR_UNRECOGNIZED_AFTER_QUERY_P(X) \
    HANDLE_PCRE2_ERROR_MISSING_NAME_TERMINATOR(X) \
    HANDLE_PCRE2_ERROR_DUPLICATE_SUBPATTERN_NAME(X) \
    HANDLE_PCRE2_ERROR_INVALID_SUBPATTERN_NAME(X) \
    HANDLE_PCRE2_ERROR_UNICODE_PROPERTIES_UNAVAILABLE(X) \
    HANDLE_PCRE2_ERROR_MALFORMED_UNICODE_PROPERTY(X) \
    HANDLE_PCRE2_ERROR_UNKNOWN_UNICODE_PROPERTY(X) \
    HANDLE_PCRE2_ERROR_SUBPATTERN_NAME_TOO_LONG(X) \
    HANDLE_PCRE2_ERROR_TOO_MANY_NAMED_SUBPATTERNS(X) \
    HANDLE_PCRE2_ERROR_CLASS_INVALID_RANGE(X) \
    HANDLE_PCRE2_ERROR_OCTAL_BYTE_TOO_BIG(X) \
    HANDLE_PCRE2_ERROR_INTERNAL_OVERRAN_WORKSPACE(X) \
    HANDLE_PCRE2_ERROR_INTERNAL_MISSING_SUBPATTERN(X) \
    HANDLE_PCRE2_ERROR_DEFINE_TOO_MANY_BRANCHES(X) \
    HANDLE_PCRE2_ERROR_BACKSLASH_O_MISSING_BRACE(X) \
    HANDLE_PCRE2_ERROR_INTERNAL_UNKNOWN_NEWLINE(X) \
    HANDLE_PCRE2_ERROR_BACKSLASH_G_SYNTAX(X) \
    HANDLE_PCRE2_ERROR_PARENS_QUERY_R_MISSING_CLOSING(X) \
    HANDLE_PCRE2_ERROR_VERB_ARGUMENT_NOT_ALLOWED(X) \
    HANDLE_PCRE2_ERROR_VERB_UNKNOWN(X) \
    HANDLE_PCRE2_ERROR_SUBPATTERN_NUMBER_TOO_BIG(X) \
    HANDLE_PCRE2_ERROR_SUBPATTERN_NAME_EXPECTED(X) \
    HANDLE_PCRE2_ERROR_INTERNAL_PARSED_OVERFLOW(X) \
    HANDLE_PCRE2_ERROR_INVALID_OCTAL(X) \
    HANDLE_PCRE2_ERROR_SUBPATTERN_NAMES_MISMATCH(X) \
    HANDLE_PCRE2_ERROR_MARK_MISSING_ARGUMENT(X) \
    HANDLE_PCRE2_ERROR_INVALID_HEXADECIMAL(X) \
    HANDLE_PCRE2_ERROR_BACKSLASH_C_SYNTAX(X) \
    HANDLE_PCRE2_ERROR_BACKSLASH_K_SYNTAX(X) \
    HANDLE_PCRE2_ERROR_INTERNAL_BAD_CODE_LOOKBEHINDS(X) \
    HANDLE_PCRE2_ERROR_BACKSLASH_N_IN_CLASS(X) \
    HANDLE_PCRE2_ERROR_CALLOUT_STRING_TOO_LONG(X) \
    HANDLE_PCRE2_ERROR_UNICODE_DISALLOWED_CODE_POINT(X) \
    HANDLE_PCRE2_ERROR_UTF_IS_DISABLED(X) \
    HANDLE_PCRE2_ERROR_UCP_IS_DISABLED(X) \
    HANDLE_PCRE2_ERROR_VERB_NAME_TOO_LONG(X) \
    HANDLE_PCRE2_ERROR_BACKSLASH_U_CODE_POINT_TOO_BIG(X) \
    HANDLE_PCRE2_ERROR_MISSING_OCTAL_OR_HEX_DIGITS(X) \
    HANDLE_PCRE2_ERROR_VERSION_CONDITION_SYNTAX(X) \
    HANDLE_PCRE2_ERROR_INTERNAL_BAD_CODE_AUTO_POSSESS(X) \
    HANDLE_PCRE2_ERROR_CALLOUT_NO_STRING_DELIMITER(X) \
    HANDLE_PCRE2_ERROR_CALLOUT_BAD_STRING_DELIMITER(X) \
    HANDLE_PCRE2_ERROR_BACKSLASH_C_CALLER_DISABLED(X) \
    HANDLE_PCRE2_ERROR_QUERY_BARJX_NEST_TOO_DEEP(X) \
    HANDLE_PCRE2_ERROR_BACKSLASH_C_LIBRARY_DISABLED(X) \
    HANDLE_PCRE2_ERROR_PATTERN_TOO_COMPLICATED(X) \
    HANDLE_PCRE2_ERROR_LOOKBEHIND_TOO_LONG(X) \
    HANDLE_PCRE2_ERROR_PATTERN_STRING_TOO_LONG(X) \
    HANDLE_PCRE2_ERROR_INTERNAL_BAD_CODE(X) \
    HANDLE_PCRE2_ERROR_INTERNAL_BAD_CODE_IN_SKIP(X) \
    HANDLE_PCRE2_ERROR_NO_SURROGATES_IN_UTF16(X) \
    HANDLE_PCRE2_ERROR_BAD_LITERAL_OPTIONS(X) \
    HANDLE_PCRE2_ERROR_SUPPORTED_ONLY_IN_UNICODE(X) \
    HANDLE_PCRE2_ERROR_INVALID_HYPHEN_IN_OPTIONS(X) \
    HANDLE_PCRE2_ERROR_ALPHA_ASSERTION_UNKNOWN(X) \
    HANDLE_PCRE2_ERROR_SCRIPT_RUN_NOT_AVAILABLE(X) \
    HANDLE_PCRE2_ERROR_TOO_MANY_CAPTURES(X) \
    HANDLE_PCRE2_ERROR_MISSING_OCTAL_DIGIT(X) \
    HANDLE_PCRE2_ERROR_BACKSLASH_K_IN_LOOKAROUND(X) \
    HANDLE_PCRE2_ERROR_MAX_VAR_LOOKBEHIND_EXCEEDED(X) \
    HANDLE_PCRE2_ERROR_PATTERN_COMPILED_SIZE_TOO_BIG(X) \
    HANDLE_PCRE2_ERROR_OVERSIZE_PYTHON_OCTAL(X) \
    HANDLE_PCRE2_ERROR_CALLOUT_CALLER_DISABLED(X) \
    HANDLE_PCRE2_ERROR_EXTRA_CASING_REQUIRES_UNICODE(X) \
    HANDLE_PCRE2_ERROR_TURKISH_CASING_REQUIRES_UTF(X) \
    HANDLE_PCRE2_ERROR_EXTRA_CASING_INCOMPATIBLE(X) \
    HANDLE_PCRE2_ERROR_ECLASS_NEST_TOO_DEEP(X) \
    HANDLE_PCRE2_ERROR_ECLASS_INVALID_OPERATOR(X) \
    HANDLE_PCRE2_ERROR_ECLASS_UNEXPECTED_OPERATOR(X) \
    HANDLE_PCRE2_ERROR_ECLASS_EXPECTED_OPERAND(X) \
    HANDLE_PCRE2_ERROR_ECLASS_MIXED_OPERATORS(X) \
    HANDLE_PCRE2_ERROR_ECLASS_HINT_SQUARE_BRACKET(X) \
    HANDLE_PCRE2_ERROR_PERL_ECLASS_UNEXPECTED_EXPR(X) \
    HANDLE_PCRE2_ERROR_PERL_ECLASS_EMPTY_EXPR(X) \
    HANDLE_PCRE2_ERROR_PERL_ECLASS_MISSING_CLOSE(X) \
    HANDLE_PCRE2_ERROR_PERL_ECLASS_UNEXPECTED_CHAR(X) \
    HANDLE_PCRE2_ERROR_EXPECTED_CAPTURE_GROUP(X) \
    HANDLE_PCRE2_ERROR_MISSING_OPENING_PARENTHESIS(X) \
    HANDLE_PCRE2_ERROR_MISSING_NUMBER_TERMINATOR(X) \
    HANDLE_PCRE2_ERROR_NULL_ERROROFFSET(X) \
    HANDLE_PCRE2_ERROR_NOMATCH(X) \
    HANDLE_PCRE2_ERROR_PARTIAL(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR1(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR2(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR3(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR4(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR5(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR6(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR7(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR8(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR9(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR10(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR11(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR12(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR13(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR14(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR15(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR16(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR17(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR18(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR19(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR20(X) \
    HANDLE_PCRE2_ERROR_UTF8_ERR21(X) \
    HANDLE_PCRE2_ERROR_UTF16_ERR1(X) \
    HANDLE_PCRE2_ERROR_UTF16_ERR2(X) \
    HANDLE_PCRE2_ERROR_UTF16_ERR3(X) \
    HANDLE_PCRE2_ERROR_UTF32_ERR1(X) \
    HANDLE_PCRE2_ERROR_UTF32_ERR2(X) \
    HANDLE_PCRE2_ERROR_BADDATA(X) \
    HANDLE_PCRE2_ERROR_MIXEDTABLES(X) \
    HANDLE_PCRE2_ERROR_BADMAGIC(X) \
    HANDLE_PCRE2_ERROR_BADMODE(X) \
    HANDLE_PCRE2_ERROR_BADOFFSET(X) \
    HANDLE_PCRE2_ERROR_BADOPTION(X) \
    HANDLE_PCRE2_ERROR_BADREPLACEMENT(X) \
    HANDLE_PCRE2_ERROR_BADUTFOFFSET(X) \
    HANDLE_PCRE2_ERROR_CALLOUT(X) \
    HANDLE_PCRE2_ERROR_DFA_BADRESTART(X) \
    HANDLE_PCRE2_ERROR_DFA_RECURSE(X) \
    HANDLE_PCRE2_ERROR_DFA_UCOND(X) \
    HANDLE_PCRE2_ERROR_DFA_UFUNC(X) \
    HANDLE_PCRE2_ERROR_DFA_UITEM(X) \
    HANDLE_PCRE2_ERROR_DFA_WSSIZE(X) \
    HANDLE_PCRE2_ERROR_INTERNAL(X) \
    HANDLE_PCRE2_ERROR_JIT_BADOPTION(X) \
    HANDLE_PCRE2_ERROR_JIT_STACKLIMIT(X) \
    HANDLE_PCRE2_ERROR_MATCHLIMIT(X) \
    HANDLE_PCRE2_ERROR_NOMEMORY(X) \
    HANDLE_PCRE2_ERROR_NOSUBSTRING(X) \
    HANDLE_PCRE2_ERROR_NOUNIQUESUBSTRING(X) \
    HANDLE_PCRE2_ERROR_NULL(X) \
    HANDLE_PCRE2_ERROR_RECURSELOOP(X) \
    HANDLE_PCRE2_ERROR_DEPTHLIMIT(X) \
    HANDLE_PCRE2_ERROR_RECURSIONLIMIT(X) \
    HANDLE_PCRE2_ERROR_UNAVAILABLE(X) \
    HANDLE_PCRE2_ERROR_UNSET(X) \
    HANDLE_PCRE2_ERROR_BADOFFSETLIMIT(X) \
    HANDLE_PCRE2_ERROR_BADREPESCAPE(X) \
    HANDLE_PCRE2_ERROR_REPMISSINGBRACE(X) \
    HANDLE_PCRE2_ERROR_BADSUBSTITUTION(X) \
    HANDLE_PCRE2_ERROR_BADSUBSPATTERN(X) \
    HANDLE_PCRE2_ERROR_TOOMANYREPLACE(X) \
    HANDLE_PCRE2_ERROR_BADSERIALIZEDDATA(X) \
    HANDLE_PCRE2_ERROR_HEAPLIMIT(X) \
    HANDLE_PCRE2_ERROR_CONVERT_SYNTAX(X) \
    HANDLE_PCRE2_ERROR_INTERNAL_DUPMATCH(X) \
    HANDLE_PCRE2_ERROR_DFA_UINVALID_UTF(X) \
    HANDLE_PCRE2_ERROR_INVALIDOFFSET(X) \
    HANDLE_PCRE2_ERROR_JIT_UNSUPPORTED(X) \
    HANDLE_PCRE2_ERROR_REPLACECASE(X) \
    HANDLE_PCRE2_ERROR_TOOLARGEREPLACE(X) \
    HANDLE_PCRE2_ERROR_DIFFSUBSPATTERN(X) \
    HANDLE_PCRE2_ERROR_DIFFSUBSSUBJECT(X) \
    HANDLE_PCRE2_ERROR_DIFFSUBSOFFSET(X) \
    HANDLE_PCRE2_ERROR_DIFFSUBSOPTIONS(X) \


typedef struct {
    PyObject_HEAD
    pcre2_code *code;
    PyObject *pattern;        /* Original pattern (str or bytes) */
    PyObject *pattern_bytes;  /* UTF-8 encoded representation */
    PyObject *groupindex;     /* Dict[str, int] */
    uint32_t compile_options;
    uint32_t capture_count;
    int pattern_is_bytes;
    int jit_enabled;
} PatternObject;

typedef struct {
    PyObject_HEAD
    PatternObject *pattern;
    PyObject *subject;        /* Original subject (str or bytes) */
    PyObject *subject_bytes;  /* UTF-8 encoded bytes */
    Py_ssize_t *ovector;      /* 2 * ovec_count entries */
    uint32_t ovec_count;
    int subject_is_bytes;
} MatchObject;

typedef struct {
    int code;
    const char *macro_name;
} PcreErrorInfo;

static const PcreErrorInfo pcre_error_info_list[] = {
#define DECLARE_PCRE2_ERROR_INFO(name) {name, #name},
    PCRE2_ERROR_LIST(DECLARE_PCRE2_ERROR_INFO)
#undef DECLARE_PCRE2_ERROR_INFO
};

static const size_t pcre_error_info_count = sizeof(pcre_error_info_list) / sizeof(pcre_error_info_list[0]);

static PyObject *PcreError = NULL;
static PyObject *PcreErrorByCode = NULL;
static int default_jit_enabled = 1;
typedef struct MatchDataCacheEntry {
    pcre2_match_data *match_data;
    uint32_t ovec_count;
    struct MatchDataCacheEntry *next;
} MatchDataCacheEntry;

static MatchDataCacheEntry *match_data_cache_head = NULL;
static uint32_t match_data_cache_capacity = 128;
static uint32_t match_data_cache_count = 0;
static PyThread_type_lock match_data_cache_lock = NULL;

typedef struct JitStackCacheEntry {
    pcre2_jit_stack *jit_stack;
    struct JitStackCacheEntry *next;
} JitStackCacheEntry;

static JitStackCacheEntry *jit_stack_cache_head = NULL;
static uint32_t jit_stack_cache_capacity = 32;
static uint32_t jit_stack_cache_count = 0;
static PyThread_type_lock jit_stack_cache_lock = NULL;
static size_t jit_stack_start_size = 64 * 1024;
static size_t jit_stack_max_size = 1024 * 1024;

static void
match_data_cache_free_all_locked(void)
{
    MatchDataCacheEntry *node = match_data_cache_head;
    match_data_cache_head = NULL;
    match_data_cache_count = 0;

    while (node != NULL) {
        MatchDataCacheEntry *next = node->next;
        pcre2_match_data_free(node->match_data);
        PyMem_Free(node);
        node = next;
    }
}

static void
jit_stack_cache_free_all_locked(void)
{
    JitStackCacheEntry *node = jit_stack_cache_head;
    jit_stack_cache_head = NULL;
    jit_stack_cache_count = 0;

    while (node != NULL) {
        JitStackCacheEntry *next = node->next;
        pcre2_jit_stack_free(node->jit_stack);
        PyMem_Free(node);
        node = next;
    }
}

static char *
macro_name_to_camel(const char *macro_name)
{
    static const char prefix[] = "PCRE2_ERROR_";
    const size_t prefix_len = sizeof(prefix) - 1;
    const char *cursor = macro_name;

    if (strncmp(cursor, prefix, prefix_len) == 0) {
        cursor += prefix_len;
    }

    size_t length = 0;
    int new_component = 1;
    for (const char *p = cursor; *p != '\0'; ++p) {
        if (*p == '_') {
            new_component = 1;
            continue;
        }
        length++;
        new_component = 0;
    }

    char *result = PyMem_Malloc(length + 1);
    if (result == NULL) {
        PyErr_NoMemory();
        return NULL;
    }

    size_t out_index = 0;
    new_component = 1;
    for (const char *p = cursor; *p != '\0'; ++p) {
        unsigned char uc = (unsigned char)*p;
        if (uc == '_') {
            new_component = 1;
            continue;
        }
        char c = (char)uc;
        if (isalpha(uc)) {
            c = (char)(new_component ? toupper(uc) : tolower(uc));
        }
        result[out_index++] = c;
        new_component = 0;
    }
    result[out_index] = '\0';
    return result;
}

static PyObject *
get_error_type_for_code(int error_code)
{
    if (PcreErrorByCode == NULL) {
        Py_INCREF(PcreError);
        return PcreError;
    }

    PyObject *code_obj = PyLong_FromLong(error_code);
    if (code_obj == NULL) {
        return NULL;
    }

    PyObject *exc_type = PyDict_GetItemWithError(PcreErrorByCode, code_obj);
    Py_DECREF(code_obj);
    if (exc_type == NULL) {
        if (PyErr_Occurred()) {
            return NULL;
        }
        Py_INCREF(PcreError);
        return PcreError;
    }

    Py_INCREF(exc_type);
    return exc_type;
}

static int
coerce_jit_argument(PyObject *value, int *out, int *is_explicit)
{
    if (is_explicit != NULL) {
        *is_explicit = (value != NULL && value != Py_None);
    }
    if (value == NULL || value == Py_None) {
        *out = default_jit_enabled;
        return 0;
    }

    int truth = PyObject_IsTrue(value);
    if (truth < 0) {
        return -1;
    }

    *out = truth ? 1 : 0;
    return 0;
}

/* Utility helpers */
static void
raise_pcre_error(const char *context, int error_code, PCRE2_SIZE error_offset)
{
    PCRE2_UCHAR buffer[256];
    int rc = pcre2_get_error_message(error_code, buffer,
                                     (PCRE2_SIZE)(sizeof(buffer) / sizeof(PCRE2_UCHAR)));
    const char *message = rc >= 0 ? (const char *)buffer : "unknown PCRE2 error";

    PyObject *exc_type = get_error_type_for_code(error_code);
    if (exc_type == NULL) {
        return;
    }

    PyObject *exc = PyObject_CallFunction(exc_type, "ss", context, message);
    if (exc == NULL) {
        Py_DECREF(exc_type);
        return;
    }

    PyObject *code_obj = PyLong_FromLong(error_code);
    if (code_obj != NULL) {
        if (PyObject_SetAttrString(exc, "code", code_obj) < 0) {
            PyErr_Clear();
        }
    }
    Py_XDECREF(code_obj);

    PyObject *offset_obj = PyLong_FromSize_t((size_t)error_offset);
    if (offset_obj != NULL) {
        if (PyObject_SetAttrString(exc, "offset", offset_obj) < 0) {
            PyErr_Clear();
        }
    }
    Py_XDECREF(offset_obj);

    PyErr_SetObject(exc_type, exc);
    Py_DECREF(exc_type);
    Py_DECREF(exc);
}

static int
register_pcre_error_types(PyObject *module)
{
    if (PcreErrorByCode != NULL) {
        return 0;
    }

    PcreErrorByCode = PyDict_New();
    if (PcreErrorByCode == NULL) {
        return -1;
    }

    for (size_t i = 0; i < pcre_error_info_count; ++i) {
        const PcreErrorInfo *info = &pcre_error_info_list[i];
        char *camel = macro_name_to_camel(info->macro_name);
        if (camel == NULL) {
            goto error;
        }

        size_t camel_len = strlen(camel);
        size_t class_name_len = strlen("PcreError") + camel_len;
        char *class_name = PyMem_Malloc(class_name_len + 1);
        if (class_name == NULL) {
            PyErr_NoMemory();
            PyMem_Free(camel);
            goto error;
        }
        snprintf(class_name, class_name_len + 1, "PcreError%s", camel);

        size_t qual_name_len = strlen("pcre.") + class_name_len;
        char *qual_name = PyMem_Malloc(qual_name_len + 1);
        if (qual_name == NULL) {
            PyErr_NoMemory();
            PyMem_Free(class_name);
            PyMem_Free(camel);
            goto error;
        }
        snprintf(qual_name, qual_name_len + 1, "pcre.%s", class_name);

        PyObject *exc_type = PyErr_NewException(qual_name, PcreError, NULL);
        PyMem_Free(qual_name);
        PyMem_Free(camel);
        if (exc_type == NULL) {
            PyMem_Free(class_name);
            goto error;
        }

        PyObject *code_obj = PyLong_FromLong(info->code);
        if (code_obj == NULL) {
            Py_DECREF(exc_type);
            PyMem_Free(class_name);
            goto error;
        }

        if (PyObject_SetAttrString(exc_type, "code", code_obj) < 0) {
            Py_DECREF(code_obj);
            Py_DECREF(exc_type);
            PyMem_Free(class_name);
            goto error;
        }

        PyObject *macro_str = PyUnicode_FromString(info->macro_name);
        if (macro_str == NULL) {
            Py_DECREF(code_obj);
            Py_DECREF(exc_type);
            PyMem_Free(class_name);
            goto error;
        }
        if (PyObject_SetAttrString(exc_type, "macro", macro_str) < 0) {
            Py_DECREF(macro_str);
            Py_DECREF(code_obj);
            Py_DECREF(exc_type);
            PyMem_Free(class_name);
            goto error;
        }
        Py_DECREF(macro_str);

        int contains = PyDict_Contains(PcreErrorByCode, code_obj);
        if (contains < 0) {
            Py_DECREF(code_obj);
            Py_DECREF(exc_type);
            PyMem_Free(class_name);
            goto error;
        }
        if (!contains) {
            if (PyDict_SetItem(PcreErrorByCode, code_obj, exc_type) < 0) {
                Py_DECREF(code_obj);
                Py_DECREF(exc_type);
                PyMem_Free(class_name);
                goto error;
            }
        }

        if (PyModule_AddObject(module, class_name, exc_type) < 0) {
            if (!contains) {
                PyDict_DelItem(PcreErrorByCode, code_obj);
            }
            Py_DECREF(code_obj);
            Py_DECREF(exc_type);
            PyMem_Free(class_name);
            goto error;
        }
        PyMem_Free(class_name);
        Py_DECREF(code_obj);
    }

#define ADD_PCRE2_ERROR_CONSTANT(name)                                                     \
    if (PyModule_AddIntConstant(module, #name, name) < 0) {                               \
        goto error;                                                                        \
    }
    PCRE2_ERROR_LIST(ADD_PCRE2_ERROR_CONSTANT)
#undef ADD_PCRE2_ERROR_CONSTANT

    return 0;

error:
    Py_CLEAR(PcreErrorByCode);
    return -1;
}

static PyObject *
bytes_from_text(PyObject *obj)
{
    if (PyBytes_Check(obj)) {
        Py_INCREF(obj);
        return obj;
    }
    if (PyUnicode_Check(obj)) {
        return PyUnicode_AsUTF8String(obj);
    }
    PyErr_SetString(PyExc_TypeError, "expected str or bytes");
    return NULL;
}

static Py_ssize_t
utf8_offset_to_index(const char *data, Py_ssize_t length)
{
    PyObject *tmp = PyUnicode_DecodeUTF8(data, length, "strict");
    if (tmp == NULL) {
        return -1;
    }
    Py_ssize_t index = PyUnicode_GET_LENGTH(tmp);
    Py_DECREF(tmp);
    return index;
}

static int
utf8_index_to_offset(PyObject *unicode_obj, Py_ssize_t index, Py_ssize_t *offset_out)
{
    if (!PyUnicode_Check(unicode_obj)) {
        *offset_out = index;
        return 0;
    }

    if (PyUnicode_READY(unicode_obj) < 0) {
        return -1;
    }

    Py_ssize_t length = PyUnicode_GET_LENGTH(unicode_obj);
    if (index < 0) {
        index += length;
        if (index < 0) {
            index = 0;
        }
    }
    if (index > length) {
        index = length;
    }

    int kind = PyUnicode_KIND(unicode_obj);
    void *data = PyUnicode_DATA(unicode_obj);
    Py_ssize_t offset = 0;

    for (Py_ssize_t i = 0; i < index; ++i) {
        Py_UCS4 ch = PyUnicode_READ(kind, data, i);
        if (ch <= 0x7F) {
            offset += 1;
        } else if (ch <= 0x7FF) {
            offset += 2;
        } else if (ch <= 0xFFFF) {
            offset += 3;
        } else {
            offset += 4;
        }
    }

    *offset_out = offset;
    return 0;
}

static PyObject *
create_groupindex_dict(pcre2_code *code)
{
    uint32_t namecount = 0;
    if (pcre2_pattern_info(code, PCRE2_INFO_NAMECOUNT, &namecount) != 0 || namecount == 0) {
        return PyDict_New();
    }

    uint32_t entry_size = 0;
    if (pcre2_pattern_info(code, PCRE2_INFO_NAMEENTRYSIZE, &entry_size) != 0) {
        return PyDict_New();
    }

    PCRE2_SPTR table = NULL;
    if (pcre2_pattern_info(code, PCRE2_INFO_NAMETABLE, &table) != 0 || table == NULL) {
        return PyDict_New();
    }

    PyObject *mapping = PyDict_New();
    if (mapping == NULL) {
        return NULL;
    }

    for (uint32_t i = 0; i < namecount; ++i) {
        const unsigned char *entry = (const unsigned char *)(table + i * entry_size);
        uint16_t number = (uint16_t)((entry[0] << 8) | entry[1]);
        const char *name = (const char *)(entry + 2);

        PyObject *key = PyUnicode_FromString(name);
        PyObject *value = PyLong_FromUnsignedLong((unsigned long)number);
        if (key == NULL || value == NULL) {
            Py_XDECREF(key);
            Py_XDECREF(value);
            Py_DECREF(mapping);
            return NULL;
        }
        if (PyDict_SetItem(mapping, key, value) < 0) {
            Py_DECREF(key);
            Py_DECREF(value);
            Py_DECREF(mapping);
            return NULL;
        }
        Py_DECREF(key);
        Py_DECREF(value);
    }

    return mapping;
}

/* Match type */
static void
Match_dealloc(MatchObject *self)
{
    Py_XDECREF(self->pattern);
    Py_XDECREF(self->subject);
    Py_XDECREF(self->subject_bytes);
    PyMem_Free(self->ovector);
    Py_TYPE(self)->tp_free((PyObject *)self);
}

static PyObject *
Match_repr(MatchObject *self)
{
    Py_ssize_t start = self->ovector[0];
    Py_ssize_t end = self->ovector[1];
    return PyUnicode_FromFormat("<Match span=(%zd, %zd) pattern=%R>", start, end, self->pattern->pattern);
}

static int
resolve_group_key(MatchObject *self, PyObject *key, Py_ssize_t *index)
{
    if (key == NULL) {
        *index = 0;
        return 0;
    }
    if (PyLong_Check(key)) {
        Py_ssize_t value = PyLong_AsSsize_t(key);
        if (value == -1 && PyErr_Occurred()) {
            return -1;
        }
        *index = value;
        return 0;
    }
    if (PyUnicode_Check(key)) {
        PyObject *item = PyDict_GetItemWithError(self->pattern->groupindex, key);
        if (item == NULL) {
            if (!PyErr_Occurred()) {
                PyErr_Format(PyExc_IndexError, "no such group '%U'", key);
            }
            return -1;
        }
        Py_ssize_t value = PyLong_AsSsize_t(item);
        if (value == -1 && PyErr_Occurred()) {
            return -1;
        }
        *index = value;
        return 0;
    }
    PyErr_SetString(PyExc_TypeError, "group indices must be integers or strings");
    return -1;
}

static PyObject *
match_get_group_value(MatchObject *self, Py_ssize_t index)
{
    if (index < 0 || (uint32_t)index >= self->ovec_count) {
        PyErr_SetString(PyExc_IndexError, "group index out of range");
        return NULL;
    }
    Py_ssize_t start = self->ovector[index * 2];
    Py_ssize_t end = self->ovector[index * 2 + 1];
    if (start < 0 || end < 0) {
        Py_RETURN_NONE;
    }

    const char *data = PyBytes_AS_STRING(self->subject_bytes);
    Py_ssize_t length = end - start;
    if (self->subject_is_bytes) {
        return PyBytes_FromStringAndSize(data + start, length);
    }
    return PyUnicode_DecodeUTF8(data + start, length, "strict");
}

static PyObject *
Match_group(MatchObject *self, PyObject *args)
{
    Py_ssize_t nargs = PyTuple_GET_SIZE(args);
    if (nargs == 0) {
        return match_get_group_value(self, 0);
    }
    if (nargs == 1) {
        PyObject *key = PyTuple_GET_ITEM(args, 0);
        Py_ssize_t index = 0;
        if (resolve_group_key(self, key, &index) < 0) {
            return NULL;
        }
        return match_get_group_value(self, index);
    }
    PyObject *result = PyTuple_New(nargs);
    if (result == NULL) {
        return NULL;
    }
    for (Py_ssize_t i = 0; i < nargs; ++i) {
        PyObject *key = PyTuple_GET_ITEM(args, i);
        Py_ssize_t index = 0;
        if (resolve_group_key(self, key, &index) < 0) {
            Py_DECREF(result);
            return NULL;
        }
        PyObject *value = match_get_group_value(self, index);
        if (value == NULL) {
            Py_DECREF(result);
            return NULL;
        }
        PyTuple_SET_ITEM(result, i, value);
    }
    return result;
}

static PyObject *
Match_groups(MatchObject *self, PyObject *args, PyObject *kwargs)
{
    static char *kwlist[] = {"default", NULL};
    PyObject *default_value = Py_None;
    if (!PyArg_ParseTupleAndKeywords(args, kwargs, "|O", kwlist, &default_value)) {
        return NULL;
    }

    PyObject *result = PyTuple_New(self->ovec_count - 1);
    if (result == NULL) {
        return NULL;
    }

    for (uint32_t i = 1; i < self->ovec_count; ++i) {
        PyObject *value = match_get_group_value(self, (Py_ssize_t)i);
        if (value == NULL) {
            Py_DECREF(result);
            return NULL;
        }
        if (value == Py_None && default_value != Py_None) {
            Py_DECREF(value);
            Py_INCREF(default_value);
            value = default_value;
        }
        PyTuple_SET_ITEM(result, i - 1, value);
    }

    return result;
}

static PyObject *
Match_span(MatchObject *self, PyObject *args)
{
    PyObject *key = NULL;
    if (!PyArg_ParseTuple(args, "|O", &key)) {
        return NULL;
    }
    Py_ssize_t index = 0;
    if (resolve_group_key(self, key, &index) < 0) {
        return NULL;
    }
    if (index < 0 || (uint32_t)index >= self->ovec_count) {
        PyErr_SetString(PyExc_IndexError, "group index out of range");
        return NULL;
    }
    Py_ssize_t start = self->ovector[index * 2];
    Py_ssize_t end = self->ovector[index * 2 + 1];
    if (start < 0 || end < 0) {
        Py_RETURN_NONE;
    }
    if (self->subject_is_bytes) {
        return Py_BuildValue("(nn)", start, end);
    }
    const char *data = PyBytes_AS_STRING(self->subject_bytes);
    Py_ssize_t start_index = utf8_offset_to_index(data, start);
    if (start_index < 0 && PyErr_Occurred()) {
        return NULL;
    }
    Py_ssize_t end_index = utf8_offset_to_index(data, end);
    if (end_index < 0 && PyErr_Occurred()) {
        return NULL;
    }
    return Py_BuildValue("(nn)", start_index, end_index);
}

static PyObject *
Match_start(MatchObject *self, PyObject *args)
{
    PyObject *span = Match_span(self, args);
    if (span == NULL || span == Py_None) {
        return span;
    }
    PyObject *value = PyTuple_GET_ITEM(span, 0);
    Py_INCREF(value);
    Py_DECREF(span);
    return value;
}

static PyObject *
Match_end(MatchObject *self, PyObject *args)
{
    PyObject *span = Match_span(self, args);
    if (span == NULL || span == Py_None) {
        return span;
    }
    PyObject *value = PyTuple_GET_ITEM(span, 1);
    Py_INCREF(value);
    Py_DECREF(span);
    return value;
}

static PyObject *
Match_groupdict(MatchObject *self, PyObject *args, PyObject *kwargs)
{
    static char *kwlist[] = {"default", NULL};
    PyObject *default_value = Py_None;
    if (!PyArg_ParseTupleAndKeywords(args, kwargs, "|O", kwlist, &default_value)) {
        return NULL;
    }

    PyObject *result = PyDict_New();
    if (result == NULL) {
        return NULL;
    }

    PyObject *key, *value;
    Py_ssize_t pos = 0;
    while (PyDict_Next(self->pattern->groupindex, &pos, &key, &value)) {
        Py_ssize_t index = PyLong_AsSsize_t(value);
        if (index == -1 && PyErr_Occurred()) {
            Py_DECREF(result);
            return NULL;
        }
        PyObject *group_args = Py_BuildValue("(n)", index);
        if (group_args == NULL) {
            Py_DECREF(result);
            return NULL;
        }
        PyObject *group_value = Match_group(self, group_args);
        Py_DECREF(group_args);
        if (group_value == NULL) {
            Py_DECREF(result);
            return NULL;
        }
        if (group_value == Py_None && default_value != Py_None) {
            Py_DECREF(group_value);
            Py_INCREF(default_value);
            group_value = default_value;
        }
        if (PyDict_SetItem(result, key, group_value) < 0) {
            Py_DECREF(group_value);
            Py_DECREF(result);
            return NULL;
        }
        Py_DECREF(group_value);
    }

    return result;
}

static PyObject *
Match_get_string(MatchObject *self, void *closure)
{
    Py_INCREF(self->subject);
    return self->subject;
}

static PyMethodDef Match_methods[] = {
    {"group", (PyCFunction)Match_group, METH_VARARGS, PyDoc_STR("Return one or more capture groups.")},
    {"groups", (PyCFunction)Match_groups, METH_VARARGS | METH_KEYWORDS, PyDoc_STR("Return all capture groups as a tuple." )},
    {"groupdict", (PyCFunction)Match_groupdict, METH_VARARGS | METH_KEYWORDS, PyDoc_STR("Return a dict for named capture groups." )},
    {"span", (PyCFunction)Match_span, METH_VARARGS, PyDoc_STR("Return the (start, end) span for a group." )},
    {"start", (PyCFunction)Match_start, METH_VARARGS, PyDoc_STR("Return the start index for a group." )},
    {"end", (PyCFunction)Match_end, METH_VARARGS, PyDoc_STR("Return the end index for a group." )},
    {NULL, NULL, 0, NULL},
};

static PyGetSetDef Match_getset[] = {
    {"string", (getter)Match_get_string, NULL, PyDoc_STR("Original subject."), NULL},
    {NULL, NULL, NULL, NULL, NULL},
};

static PyTypeObject MatchType = {
    PyVarObject_HEAD_INIT(NULL, 0)
    .tp_name = "pcre.Match",
    .tp_basicsize = sizeof(MatchObject),
    .tp_dealloc = (destructor)Match_dealloc,
    .tp_repr = (reprfunc)Match_repr,
    .tp_flags = Py_TPFLAGS_DEFAULT,
    .tp_methods = Match_methods,
    .tp_getset = Match_getset,
    .tp_doc = "Match object returned by PCRE2 operations.",
};

/* Pattern helpers */
static MatchObject *
create_match_object(PatternObject *pattern,
                    PyObject *subject_obj,
                    PyObject *subject_bytes,
                    uint32_t ovec_count,
                    PCRE2_SIZE *ovector)
{
    MatchObject *match = PyObject_New(MatchObject, &MatchType);
    if (match == NULL) {
        return NULL;
    }

    match->ovector = PyMem_Malloc(sizeof(Py_ssize_t) * ovec_count * 2);
    if (match->ovector == NULL) {
        PyErr_NoMemory();
        PyObject_Del(match);
        return NULL;
    }

    for (uint32_t i = 0; i < ovec_count * 2; ++i) {
        match->ovector[i] = (Py_ssize_t)ovector[i];
    }
    match->ovec_count = ovec_count;

    Py_INCREF(pattern);
    match->pattern = pattern;

    Py_INCREF(subject_obj);
    match->subject = subject_obj;

    match->subject_bytes = subject_bytes;
    match->subject_is_bytes = PyBytes_Check(subject_obj);

    return match;
}

static void
match_data_cache_evict_tail_locked(void)
{
    if (match_data_cache_head == NULL) {
        return;
    }

    MatchDataCacheEntry *prev = NULL;
    MatchDataCacheEntry *node = match_data_cache_head;
    while (node->next != NULL) {
        prev = node;
        node = node->next;
    }

    if (prev != NULL) {
        prev->next = NULL;
    } else {
        match_data_cache_head = NULL;
    }

    if (match_data_cache_count > 0) {
        match_data_cache_count--;
    }

    pcre2_match_data_free(node->match_data);
    PyMem_Free(node);
}

static pcre2_match_data *
match_data_cache_acquire(PatternObject *self)
{
    uint32_t required_pairs = self->capture_count + 1;
    if (required_pairs == 0) {
        required_pairs = 1;
    }

    pcre2_match_data *cached = NULL;

    if (match_data_cache_lock != NULL) {
        PyThread_acquire_lock(match_data_cache_lock, 1);
    }

    if (match_data_cache_capacity != 0) {
        MatchDataCacheEntry **link = &match_data_cache_head;
        MatchDataCacheEntry *entry = match_data_cache_head;
        while (entry != NULL) {
            if (entry->ovec_count >= required_pairs) {
                *link = entry->next;
                if (match_data_cache_count > 0) {
                    match_data_cache_count--;
                }
                cached = entry->match_data;
                PyMem_Free(entry);
                break;
            }
            link = &entry->next;
            entry = entry->next;
        }

        if (cached == NULL && match_data_cache_count >= match_data_cache_capacity) {
            match_data_cache_evict_tail_locked();
        }
    }

    if (match_data_cache_lock != NULL) {
        PyThread_release_lock(match_data_cache_lock);
    }

    if (cached != NULL) {
        return cached;
    }

    pcre2_match_data *match_data = pcre2_match_data_create(required_pairs, NULL);
    if (match_data != NULL) {
        return match_data;
    }
    return pcre2_match_data_create_from_pattern(self->code, NULL);
}

static void
match_data_cache_release(pcre2_match_data *match_data)
{
    if (match_data == NULL) {
        return;
    }

    if (match_data_cache_lock != NULL) {
        PyThread_acquire_lock(match_data_cache_lock, 1);
    }

    if (match_data_cache_capacity == 0) {
        if (match_data_cache_lock != NULL) {
            PyThread_release_lock(match_data_cache_lock);
        }
        pcre2_match_data_free(match_data);
        return;
    }

    MatchDataCacheEntry *entry = PyMem_Malloc(sizeof(*entry));
    if (entry == NULL) {
        if (match_data_cache_lock != NULL) {
            PyThread_release_lock(match_data_cache_lock);
        }
        pcre2_match_data_free(match_data);
        return;
    }

    entry->match_data = match_data;
    entry->ovec_count = pcre2_get_ovector_count(match_data);
    entry->next = match_data_cache_head;
    match_data_cache_head = entry;
    match_data_cache_count++;

    while (match_data_cache_count > match_data_cache_capacity) {
        match_data_cache_evict_tail_locked();
    }

    if (match_data_cache_lock != NULL) {
        PyThread_release_lock(match_data_cache_lock);
    }
}

static void
jit_stack_cache_evict_tail_locked(void)
{
    if (jit_stack_cache_head == NULL) {
        return;
    }

    JitStackCacheEntry *prev = NULL;
    JitStackCacheEntry *node = jit_stack_cache_head;
    while (node->next != NULL) {
        prev = node;
        node = node->next;
    }

    if (prev != NULL) {
        prev->next = NULL;
    } else {
        jit_stack_cache_head = NULL;
    }

    if (jit_stack_cache_count > 0) {
        jit_stack_cache_count--;
    }

    pcre2_jit_stack_free(node->jit_stack);
    PyMem_Free(node);
}

static pcre2_jit_stack *
jit_stack_cache_acquire(void)
{
    pcre2_jit_stack *stack = NULL;

    if (jit_stack_cache_lock != NULL) {
        PyThread_acquire_lock(jit_stack_cache_lock, 1);
    }

    if (jit_stack_cache_head != NULL) {
        JitStackCacheEntry *entry = jit_stack_cache_head;
        jit_stack_cache_head = entry->next;
        if (jit_stack_cache_count > 0) {
            jit_stack_cache_count--;
        }
        stack = entry->jit_stack;
        PyMem_Free(entry);
    }

    if (jit_stack_cache_lock != NULL) {
        PyThread_release_lock(jit_stack_cache_lock);
    }

    if (stack != NULL) {
        return stack;
    }

    return pcre2_jit_stack_create(jit_stack_start_size, jit_stack_max_size, NULL);
}

static void
jit_stack_cache_release(pcre2_jit_stack *jit_stack)
{
    if (jit_stack == NULL) {
        return;
    }

    if (jit_stack_cache_lock != NULL) {
        PyThread_acquire_lock(jit_stack_cache_lock, 1);
    }

    if (jit_stack_cache_capacity == 0) {
        if (jit_stack_cache_lock != NULL) {
            PyThread_release_lock(jit_stack_cache_lock);
        }
        pcre2_jit_stack_free(jit_stack);
        return;
    }

    JitStackCacheEntry *entry = PyMem_Malloc(sizeof(*entry));
    if (entry == NULL) {
        if (jit_stack_cache_lock != NULL) {
            PyThread_release_lock(jit_stack_cache_lock);
        }
        pcre2_jit_stack_free(jit_stack);
        return;
    }

    entry->jit_stack = jit_stack;
    entry->next = jit_stack_cache_head;
    jit_stack_cache_head = entry;
    jit_stack_cache_count++;

    while (jit_stack_cache_count > jit_stack_cache_capacity) {
        jit_stack_cache_evict_tail_locked();
    }

    if (jit_stack_cache_lock != NULL) {
        PyThread_release_lock(jit_stack_cache_lock);
    }
}

typedef enum {
    EXEC_MODE_MATCH,
    EXEC_MODE_SEARCH,
    EXEC_MODE_FULLMATCH
} execute_mode;

static void
Pattern_dealloc(PatternObject *self)
{
    if (self->code != NULL) {
        pcre2_code_free(self->code);
        self->code = NULL;
    }
    Py_XDECREF(self->pattern);
    Py_XDECREF(self->pattern_bytes);
    Py_XDECREF(self->groupindex);
    Py_TYPE(self)->tp_free((PyObject *)self);
}

static PyObject *
Pattern_repr(PatternObject *self)
{
    return PyUnicode_FromFormat("<Pattern pattern=%R flags=%u>", self->pattern, self->compile_options);
}

static PyObject *
Pattern_get_pattern(PatternObject *self, void *closure)
{
    Py_INCREF(self->pattern);
    return self->pattern;
}

static PyObject *
Pattern_get_pattern_bytes(PatternObject *self, void *closure)
{
    Py_INCREF(self->pattern_bytes);
    return self->pattern_bytes;
}

static PyObject *
Pattern_get_flags(PatternObject *self, void *closure)
{
    return PyLong_FromUnsignedLong(self->compile_options);
}

static PyObject *
Pattern_get_jit(PatternObject *self, void *closure)
{
    if (self->jit_enabled) {
        Py_RETURN_TRUE;
    }
    Py_RETURN_FALSE;
}

static PyObject *
Pattern_get_groupindex(PatternObject *self, void *closure)
{
    Py_INCREF(self->groupindex);
    return self->groupindex;
}

static PyObject *
Pattern_execute(PatternObject *self, PyObject *subject_obj, Py_ssize_t pos,
                Py_ssize_t endpos, uint32_t options, execute_mode mode)
{
    PyObject *subject_bytes = bytes_from_text(subject_obj);
    if (subject_bytes == NULL) {
        return NULL;
    }

    int subject_is_bytes = PyBytes_Check(subject_obj);
    Py_ssize_t subject_length_bytes = PyBytes_GET_SIZE(subject_bytes);
    Py_ssize_t logical_length = subject_is_bytes
                                    ? subject_length_bytes
                                    : PyUnicode_GET_LENGTH(subject_obj);

    if (pos < 0) {
        pos += logical_length;
        if (pos < 0) {
            pos = 0;
        }
    }
    if (pos > logical_length) {
        Py_DECREF(subject_bytes);
        Py_RETURN_NONE;
    }

    Py_ssize_t adjusted_endpos = endpos;
    if (adjusted_endpos >= 0) {
        if (adjusted_endpos > logical_length) {
            adjusted_endpos = logical_length;
        }
        if (adjusted_endpos < pos) {
            Py_DECREF(subject_bytes);
            PyErr_SetString(PyExc_ValueError, "endpos must be >= pos");
            return NULL;
        }
    }

    Py_ssize_t byte_start = pos;
    Py_ssize_t byte_end = subject_length_bytes;

    if (subject_is_bytes) {
        byte_start = pos;
        if (adjusted_endpos >= 0) {
            byte_end = adjusted_endpos;
        }
    } else {
        if (utf8_index_to_offset(subject_obj, pos, &byte_start) < 0) {
            Py_DECREF(subject_bytes);
            return NULL;
        }
        if (adjusted_endpos >= 0) {
            if (utf8_index_to_offset(subject_obj, adjusted_endpos, &byte_end) < 0) {
                Py_DECREF(subject_bytes);
                return NULL;
            }
        }
    }

    if (byte_start > byte_end) {
        Py_DECREF(subject_bytes);
        PyErr_SetString(PyExc_ValueError, "byte offset mismatch for subject");
        return NULL;
    }

    PCRE2_SIZE offset_limit = (PCRE2_SIZE)byte_end;

    uint32_t match_options = options;
    if (mode == EXEC_MODE_MATCH) {
        match_options |= PCRE2_ANCHORED;
    } else if (mode == EXEC_MODE_FULLMATCH) {
        match_options |= (PCRE2_ANCHORED | PCRE2_ENDANCHORED);
    }

    pcre2_match_data *match_data = match_data_cache_acquire(self);
    if (match_data == NULL) {
        Py_DECREF(subject_bytes);
        PyErr_NoMemory();
        return NULL;
    }

    pcre2_match_context *match_context = NULL;
    pcre2_jit_stack *jit_stack = NULL;
    int using_jit_stack = 0;
    if (offset_limit != (PCRE2_SIZE)subject_length_bytes) {
        match_context = pcre2_match_context_create(NULL);
        if (match_context == NULL) {
            match_data_cache_release(match_data);
            Py_DECREF(subject_bytes);
            PyErr_NoMemory();
            return NULL;
        }
        int ctx_rc = pcre2_set_offset_limit(match_context, offset_limit);
        if (ctx_rc < 0) {
            pcre2_match_context_free(match_context);
            match_data_cache_release(match_data);
            Py_DECREF(subject_bytes);
            raise_pcre_error("set_offset_limit", ctx_rc, 0);
            return NULL;
        }
    }

    const char *buffer = PyBytes_AS_STRING(subject_bytes);
    int rc = 0;

    if (self->jit_enabled) {
        if (match_context == NULL) {
            match_context = pcre2_match_context_create(NULL);
            if (match_context == NULL) {
                match_data_cache_release(match_data);
                Py_DECREF(subject_bytes);
                PyErr_NoMemory();
                return NULL;
            }
        }

        jit_stack = jit_stack_cache_acquire();
        if (jit_stack == NULL) {
            pcre2_match_context_free(match_context);
            match_data_cache_release(match_data);
            Py_DECREF(subject_bytes);
            PyErr_NoMemory();
            return NULL;
        }

        pcre2_jit_stack_assign(match_context, NULL, jit_stack);
        using_jit_stack = 1;

        Py_BEGIN_ALLOW_THREADS
        rc = pcre2_jit_match(self->code,
                              (PCRE2_SPTR)buffer,
                              (PCRE2_SIZE)subject_length_bytes,
                              (PCRE2_SIZE)byte_start,
                              match_options,
                              match_data,
                              match_context);
        Py_END_ALLOW_THREADS

        if (using_jit_stack) {
            jit_stack_cache_release(jit_stack);
            jit_stack = NULL;
            using_jit_stack = 0;
        }

        if (rc == PCRE2_ERROR_JIT_BADOPTION) {
            self->jit_enabled = 0;
        } else if (rc != PCRE2_ERROR_NOMATCH && rc < 0) {
            PCRE2_SIZE error_offset = pcre2_get_startchar(match_data);
            match_data_cache_release(match_data);
            if (match_context != NULL) {
                pcre2_match_context_free(match_context);
            }
            Py_DECREF(subject_bytes);
            raise_pcre_error("jit_match", rc, error_offset);
            return NULL;
        }
    }

    if (!self->jit_enabled) {
        Py_BEGIN_ALLOW_THREADS
        rc = pcre2_match(self->code,
                         (PCRE2_SPTR)buffer,
                         (PCRE2_SIZE)subject_length_bytes,
                         (PCRE2_SIZE)byte_start,
                         match_options,
                         match_data,
                         match_context);
        Py_END_ALLOW_THREADS
    }

    if (rc == PCRE2_ERROR_NOMATCH) {
        match_data_cache_release(match_data);
        if (match_context != NULL) {
            pcre2_match_context_free(match_context);
        }
        Py_DECREF(subject_bytes);
        Py_RETURN_NONE;
    }

    if (rc < 0) {
        PCRE2_SIZE error_offset = pcre2_get_startchar(match_data);
        match_data_cache_release(match_data);
        if (match_context != NULL) {
            pcre2_match_context_free(match_context);
        }
        Py_DECREF(subject_bytes);
        raise_pcre_error("match", rc, error_offset);
        return NULL;
    }

    uint32_t available_ovector_pairs = pcre2_get_ovector_count(match_data);
    PCRE2_SIZE *ovector = pcre2_get_ovector_pointer(match_data);
    if (ovector == NULL || available_ovector_pairs == 0) {
        match_data_cache_release(match_data);
        if (match_context != NULL) {
            pcre2_match_context_free(match_context);
        }
        Py_DECREF(subject_bytes);
        PyErr_SetString(PyExc_RuntimeError, "PCRE2 returned empty match data");
        return NULL;
    }

    uint64_t expected_pairs = (uint64_t)self->capture_count + 1;
    if (expected_pairs == 0 || expected_pairs > available_ovector_pairs) {
        expected_pairs = available_ovector_pairs;
    }

    MatchObject *match = create_match_object(
        self,
        subject_obj,
        subject_bytes,
        (uint32_t)expected_pairs,
        ovector);
    match_data_cache_release(match_data);
    if (match_context != NULL) {
        pcre2_match_context_free(match_context);
    }
    if (match == NULL) {
        Py_DECREF(subject_bytes);
        return NULL;
    }

    return (PyObject *)match;
}

static PyObject *
Pattern_match_method(PatternObject *self, PyObject *args, PyObject *kwargs)
{
    static char *kwlist[] = {"subject", "pos", "endpos", "options", NULL};
    PyObject *subject = NULL;
    Py_ssize_t pos = 0;
    Py_ssize_t endpos = -1;
    unsigned long options = 0;

    if (!PyArg_ParseTupleAndKeywords(args, kwargs, "O|nnk", kwlist,
                                     &subject, &pos, &endpos, &options)) {
        return NULL;
    }

    return Pattern_execute(self, subject, pos, endpos, (uint32_t)options, EXEC_MODE_MATCH);
}

static PyObject *
Pattern_search_method(PatternObject *self, PyObject *args, PyObject *kwargs)
{
    static char *kwlist[] = {"subject", "pos", "endpos", "options", NULL};
    PyObject *subject = NULL;
    Py_ssize_t pos = 0;
    Py_ssize_t endpos = -1;
    unsigned long options = 0;

    if (!PyArg_ParseTupleAndKeywords(args, kwargs, "O|nnk", kwlist,
                                     &subject, &pos, &endpos, &options)) {
        return NULL;
    }

    return Pattern_execute(self, subject, pos, endpos, (uint32_t)options, EXEC_MODE_SEARCH);
}

static PyObject *
Pattern_fullmatch_method(PatternObject *self, PyObject *args, PyObject *kwargs)
{
    static char *kwlist[] = {"subject", "pos", "endpos", "options", NULL};
    PyObject *subject = NULL;
    Py_ssize_t pos = 0;
    Py_ssize_t endpos = -1;
    unsigned long options = 0;

    if (!PyArg_ParseTupleAndKeywords(args, kwargs, "O|nnk", kwlist,
                                     &subject, &pos, &endpos, &options)) {
        return NULL;
    }

    return Pattern_execute(self, subject, pos, endpos, (uint32_t)options, EXEC_MODE_FULLMATCH);
}

static PyMethodDef Pattern_methods[] = {
    {"match", (PyCFunction)Pattern_match_method, METH_VARARGS | METH_KEYWORDS, PyDoc_STR("Match the pattern at the start of the subject.")},
    {"search", (PyCFunction)Pattern_search_method, METH_VARARGS | METH_KEYWORDS, PyDoc_STR("Search the subject for the pattern." )},
    {"fullmatch", (PyCFunction)Pattern_fullmatch_method, METH_VARARGS | METH_KEYWORDS, PyDoc_STR("Require the pattern to match the entire subject." )},
    {NULL, NULL, 0, NULL},
};

static PyGetSetDef Pattern_getset[] = {
    {"pattern", (getter)Pattern_get_pattern, NULL, PyDoc_STR("The original pattern."), NULL},
    {"pattern_bytes", (getter)Pattern_get_pattern_bytes, NULL, PyDoc_STR("UTF-8 encoded pattern."), NULL},
    {"flags", (getter)Pattern_get_flags, NULL, PyDoc_STR("Compile-time options."), NULL},
    {"jit", (getter)Pattern_get_jit, NULL, PyDoc_STR("Whether the pattern was JIT compiled."), NULL},
    {"groupindex", (getter)Pattern_get_groupindex, NULL, PyDoc_STR("Mapping of named capture groups."), NULL},
    {NULL, NULL, NULL, NULL, NULL},
};

static PyTypeObject PatternType = {
    PyVarObject_HEAD_INIT(NULL, 0)
    .tp_name = "pcre.Pattern",
    .tp_basicsize = sizeof(PatternObject),
    .tp_dealloc = (destructor)Pattern_dealloc,
    .tp_repr = (reprfunc)Pattern_repr,
    .tp_flags = Py_TPFLAGS_DEFAULT,
    .tp_methods = Pattern_methods,
    .tp_getset = Pattern_getset,
    .tp_doc = "Compiled PCRE2 pattern.",
};

static PatternObject *
Pattern_create(PyObject *pattern_obj, uint32_t options, int jit, int jit_explicit)
{
    PyObject *pattern_bytes = bytes_from_text(pattern_obj);
    if (pattern_bytes == NULL) {
        return NULL;
    }

    Py_ssize_t pattern_length = PyBytes_GET_SIZE(pattern_bytes);
    int is_bytes = PyBytes_Check(pattern_obj);

    uint32_t compile_options = options;

    int error_code;
    PCRE2_SIZE error_offset;
    pcre2_code *code = pcre2_compile((PCRE2_SPTR)PyBytes_AS_STRING(pattern_bytes),
                                     (PCRE2_SIZE)pattern_length,
                                     compile_options,
                                     &error_code,
                                     &error_offset,
                                     NULL);
    if (code == NULL) {
        raise_pcre_error("compile", error_code, error_offset);
        Py_DECREF(pattern_bytes);
        return NULL;
    }

    PatternObject *pattern = PyObject_New(PatternObject, &PatternType);
    if (pattern == NULL) {
        pcre2_code_free(code);
        Py_DECREF(pattern_bytes);
        return NULL;
    }

    pattern->code = code;
    Py_INCREF(pattern_obj);
    pattern->pattern = pattern_obj;
    pattern->pattern_bytes = pattern_bytes;
    pattern->pattern_is_bytes = is_bytes;
    pattern->compile_options = compile_options;
    pattern->jit_enabled = 0;

    uint32_t capture_count = 0;
    if (pcre2_pattern_info(code, PCRE2_INFO_CAPTURECOUNT, &capture_count) != 0) {
        capture_count = 0;
    }
    pattern->capture_count = capture_count;

    pattern->groupindex = create_groupindex_dict(code);
    if (pattern->groupindex == NULL) {
        Py_DECREF(pattern);
        return NULL;
    }

    if (jit) {
        int jit_rc = pcre2_jit_compile(code, PCRE2_JIT_COMPLETE);
        if (jit_rc == 0) {
            pattern->jit_enabled = 1;
        } else if (jit_rc == PCRE2_ERROR_JIT_BADOPTION) {
            pattern->jit_enabled = 0;
#ifdef PCRE2_ERROR_JIT_UNSUPPORTED
        } else if (!jit_explicit && jit_rc == PCRE2_ERROR_JIT_UNSUPPORTED) {
            pattern->jit_enabled = 0;
#endif
        } else {
            Py_DECREF(pattern);
            raise_pcre_error("jit_compile", jit_rc, 0);
            return NULL;
        }
    }

    return pattern;
}

static PyObject *
module_compile(PyObject *Py_UNUSED(module), PyObject *args, PyObject *kwargs)
{
    static char *kwlist[] = {"pattern", "flags", "jit", NULL};
    PyObject *pattern = NULL;
    unsigned long flags = 0;
    PyObject *jit_obj = Py_None;
    if (!PyArg_ParseTupleAndKeywords(args, kwargs, "O|k$O", kwlist, &pattern, &flags, &jit_obj)) {
        return NULL;
    }

    int jit = 0;
    int jit_explicit = 0;
    if (coerce_jit_argument(jit_obj, &jit, &jit_explicit) < 0) {
        return NULL;
    }

    return (PyObject *)Pattern_create(pattern, (uint32_t)flags, jit, jit_explicit);
}

static PyObject *
call_pattern_method(PatternObject *pattern, PyObject *callable, PyObject *subject)
{
    PyObject *args = PyTuple_Pack(1, subject);
    if (args == NULL) {
        return NULL;
    }
    PyObject *result = PyObject_Call(callable, args, NULL);
    Py_DECREF(args);
    return result;
}

static PyObject *
module_match(PyObject *Py_UNUSED(module), PyObject *args, PyObject *kwargs)
{
    static char *kwlist[] = {"pattern", "string", "flags", "jit", NULL};
    PyObject *pattern_obj = NULL;
    PyObject *subject = NULL;
    unsigned long flags = 0;
    PyObject *jit_obj = Py_None;
    if (!PyArg_ParseTupleAndKeywords(args, kwargs, "OO|k$O", kwlist, &pattern_obj, &subject, &flags, &jit_obj)) {
        return NULL;
    }

    int jit = 0;
    int jit_explicit = 0;
    if (coerce_jit_argument(jit_obj, &jit, &jit_explicit) < 0) {
        return NULL;
    }

    PatternObject *pattern = Pattern_create(pattern_obj, (uint32_t)flags, jit, jit_explicit);
    if (pattern == NULL) {
        return NULL;
    }

    PyObject *callable = PyObject_GetAttrString((PyObject *)pattern, "match");
    if (callable == NULL) {
        Py_DECREF(pattern);
        return NULL;
    }
    PyObject *result = call_pattern_method(pattern, callable, subject);
    Py_DECREF(callable);
    Py_DECREF(pattern);
    return result;
}

static PyObject *
module_search(PyObject *Py_UNUSED(module), PyObject *args, PyObject *kwargs)
{
    static char *kwlist[] = {"pattern", "string", "flags", "jit", NULL};
    PyObject *pattern_obj = NULL;
    PyObject *subject = NULL;
    unsigned long flags = 0;
    PyObject *jit_obj = Py_None;
    if (!PyArg_ParseTupleAndKeywords(args, kwargs, "OO|k$O", kwlist, &pattern_obj, &subject, &flags, &jit_obj)) {
        return NULL;
    }

    int jit = 0;
    int jit_explicit = 0;
    if (coerce_jit_argument(jit_obj, &jit, &jit_explicit) < 0) {
        return NULL;
    }

    PatternObject *pattern = Pattern_create(pattern_obj, (uint32_t)flags, jit, jit_explicit);
    if (pattern == NULL) {
        return NULL;
    }

    PyObject *callable = PyObject_GetAttrString((PyObject *)pattern, "search");
    if (callable == NULL) {
        Py_DECREF(pattern);
        return NULL;
    }
    PyObject *result = call_pattern_method(pattern, callable, subject);
    Py_DECREF(callable);
    Py_DECREF(pattern);
    return result;
}

static PyObject *
module_fullmatch(PyObject *Py_UNUSED(module), PyObject *args, PyObject *kwargs)
{
    static char *kwlist[] = {"pattern", "string", "flags", "jit", NULL};
    PyObject *pattern_obj = NULL;
    PyObject *subject = NULL;
    unsigned long flags = 0;
    PyObject *jit_obj = Py_None;
    if (!PyArg_ParseTupleAndKeywords(args, kwargs, "OO|k$O", kwlist, &pattern_obj, &subject, &flags, &jit_obj)) {
        return NULL;
    }

    int jit = 0;
    int jit_explicit = 0;
    if (coerce_jit_argument(jit_obj, &jit, &jit_explicit) < 0) {
        return NULL;
    }

    PatternObject *pattern = Pattern_create(pattern_obj, (uint32_t)flags, jit, jit_explicit);
    if (pattern == NULL) {
        return NULL;
    }

    PyObject *callable = PyObject_GetAttrString((PyObject *)pattern, "fullmatch");
    if (callable == NULL) {
        Py_DECREF(pattern);
        return NULL;
    }
    PyObject *result = call_pattern_method(pattern, callable, subject);
    Py_DECREF(callable);
    Py_DECREF(pattern);
    return result;
}

static PyObject *
module_configure(PyObject *Py_UNUSED(module), PyObject *args, PyObject *kwargs)
{
    static char *kwlist[] = {"jit", NULL};
    PyObject *jit_obj = Py_None;
    if (!PyArg_ParseTupleAndKeywords(args, kwargs, "|O", kwlist, &jit_obj)) {
        return NULL;
    }

    if (jit_obj != Py_None) {
        int jit = 0;
        if (coerce_jit_argument(jit_obj, &jit, NULL) < 0) {
            return NULL;
        }
        default_jit_enabled = jit;
    }

    if (default_jit_enabled) {
        Py_RETURN_TRUE;
    }
    Py_RETURN_FALSE;
}

static PyObject *
module_get_match_data_cache_size(PyObject *Py_UNUSED(module), PyObject *Py_UNUSED(args))
{
    return PyLong_FromUnsignedLong((unsigned long)match_data_cache_capacity);
}

static PyObject *
module_set_match_data_cache_size(PyObject *Py_UNUSED(module), PyObject *args)
{
    unsigned long size = 0;
    if (!PyArg_ParseTuple(args, "k", &size)) {
        return NULL;
    }

    if (match_data_cache_lock != NULL) {
        PyThread_acquire_lock(match_data_cache_lock, 1);
    }

    match_data_cache_capacity = (uint32_t)size;

    while (match_data_cache_count > match_data_cache_capacity) {
        match_data_cache_evict_tail_locked();
    }

    if (match_data_cache_lock != NULL) {
        PyThread_release_lock(match_data_cache_lock);
    }

    Py_RETURN_NONE;
}

static PyObject *
module_clear_match_data_cache(PyObject *Py_UNUSED(module), PyObject *Py_UNUSED(args))
{
    if (match_data_cache_lock != NULL) {
        PyThread_acquire_lock(match_data_cache_lock, 1);
    }

    match_data_cache_free_all_locked();

    if (match_data_cache_lock != NULL) {
        PyThread_release_lock(match_data_cache_lock);
    }

    Py_RETURN_NONE;
}

static PyObject *
module_get_match_data_cache_count(PyObject *Py_UNUSED(module), PyObject *Py_UNUSED(args))
{
    unsigned long count = 0;

    if (match_data_cache_lock != NULL) {
        PyThread_acquire_lock(match_data_cache_lock, 1);
    }

    count = match_data_cache_count;

    if (match_data_cache_lock != NULL) {
        PyThread_release_lock(match_data_cache_lock);
    }

    return PyLong_FromUnsignedLong(count);
}

static PyObject *
module_get_jit_stack_cache_size(PyObject *Py_UNUSED(module), PyObject *Py_UNUSED(args))
{
    return PyLong_FromUnsignedLong((unsigned long)jit_stack_cache_capacity);
}

static PyObject *
module_set_jit_stack_cache_size(PyObject *Py_UNUSED(module), PyObject *args)
{
    unsigned long size = 0;
    if (!PyArg_ParseTuple(args, "k", &size)) {
        return NULL;
    }

    if (jit_stack_cache_lock != NULL) {
        PyThread_acquire_lock(jit_stack_cache_lock, 1);
    }

    jit_stack_cache_capacity = (uint32_t)size;

    while (jit_stack_cache_count > jit_stack_cache_capacity) {
        jit_stack_cache_evict_tail_locked();
    }

    if (jit_stack_cache_lock != NULL) {
        PyThread_release_lock(jit_stack_cache_lock);
    }

    Py_RETURN_NONE;
}

static PyObject *
module_clear_jit_stack_cache(PyObject *Py_UNUSED(module), PyObject *Py_UNUSED(args))
{
    if (jit_stack_cache_lock != NULL) {
        PyThread_acquire_lock(jit_stack_cache_lock, 1);
    }

    jit_stack_cache_free_all_locked();

    if (jit_stack_cache_lock != NULL) {
        PyThread_release_lock(jit_stack_cache_lock);
    }

    Py_RETURN_NONE;
}

static PyObject *
module_get_jit_stack_cache_count(PyObject *Py_UNUSED(module), PyObject *Py_UNUSED(args))
{
    unsigned long count = 0;

    if (jit_stack_cache_lock != NULL) {
        PyThread_acquire_lock(jit_stack_cache_lock, 1);
    }

    count = jit_stack_cache_count;

    if (jit_stack_cache_lock != NULL) {
        PyThread_release_lock(jit_stack_cache_lock);
    }

    return PyLong_FromUnsignedLong(count);
}

static PyObject *
module_get_jit_stack_limits(PyObject *Py_UNUSED(module), PyObject *Py_UNUSED(args))
{
    return Py_BuildValue("kk", (unsigned long)jit_stack_start_size, (unsigned long)jit_stack_max_size);
}

static PyObject *
module_set_jit_stack_limits(PyObject *Py_UNUSED(module), PyObject *args)
{
    unsigned long start = 0;
    unsigned long max = 0;

    if (!PyArg_ParseTuple(args, "kk", &start, &max)) {
        return NULL;
    }

    if (start == 0 || max == 0) {
        PyErr_SetString(PyExc_ValueError, "start and max must be greater than zero");
        return NULL;
    }

    if (start > max) {
        PyErr_SetString(PyExc_ValueError, "start must be <= max");
        return NULL;
    }

    if (jit_stack_cache_lock != NULL) {
        PyThread_acquire_lock(jit_stack_cache_lock, 1);
    }

    jit_stack_start_size = (size_t)start;
    jit_stack_max_size = (size_t)max;
    jit_stack_cache_free_all_locked();

    if (jit_stack_cache_lock != NULL) {
        PyThread_release_lock(jit_stack_cache_lock);
    }

    Py_RETURN_NONE;
}

static PyMethodDef module_methods[] = {
    {"compile", (PyCFunction)module_compile, METH_VARARGS | METH_KEYWORDS, PyDoc_STR("Compile a pattern into a PCRE2 Pattern object.")},
    {"match", (PyCFunction)module_match, METH_VARARGS | METH_KEYWORDS, PyDoc_STR("Match a pattern against the beginning of a string.")},
    {"search", (PyCFunction)module_search, METH_VARARGS | METH_KEYWORDS, PyDoc_STR("Search a string for a pattern." )},
    {"fullmatch", (PyCFunction)module_fullmatch, METH_VARARGS | METH_KEYWORDS, PyDoc_STR("Match a pattern against the entire string." )},
    {"configure", (PyCFunction)module_configure, METH_VARARGS | METH_KEYWORDS, PyDoc_STR("Get or set module-wide defaults (currently only 'jit')." )},
    {"get_match_data_cache_size", (PyCFunction)module_get_match_data_cache_size, METH_NOARGS, PyDoc_STR("Return the capacity of the reusable match-data cache." )},
    {"set_match_data_cache_size", (PyCFunction)module_set_match_data_cache_size, METH_VARARGS, PyDoc_STR("Set the capacity of the reusable match-data cache." )},
    {"clear_match_data_cache", (PyCFunction)module_clear_match_data_cache, METH_NOARGS, PyDoc_STR("Release all cached PCRE2 match-data buffers." )},
    {"get_match_data_cache_count", (PyCFunction)module_get_match_data_cache_count, METH_NOARGS, PyDoc_STR("Return the number of cached match-data buffers currently stored." )},
    {"get_jit_stack_cache_size", (PyCFunction)module_get_jit_stack_cache_size, METH_NOARGS, PyDoc_STR("Return the capacity of the reusable JIT stack cache." )},
    {"set_jit_stack_cache_size", (PyCFunction)module_set_jit_stack_cache_size, METH_VARARGS, PyDoc_STR("Set the capacity of the reusable JIT stack cache." )},
    {"clear_jit_stack_cache", (PyCFunction)module_clear_jit_stack_cache, METH_NOARGS, PyDoc_STR("Release all cached PCRE2 JIT stacks." )},
    {"get_jit_stack_cache_count", (PyCFunction)module_get_jit_stack_cache_count, METH_NOARGS, PyDoc_STR("Return the number of cached JIT stacks currently stored." )},
    {"get_jit_stack_limits", (PyCFunction)module_get_jit_stack_limits, METH_NOARGS, PyDoc_STR("Return the configured (start, max) JIT stack sizes." )},
    {"set_jit_stack_limits", (PyCFunction)module_set_jit_stack_limits, METH_VARARGS, PyDoc_STR("Set the (start, max) sizes for newly created JIT stacks." )},
    {NULL, NULL, 0, NULL},
};

static struct PyModuleDef moduledef = {
    PyModuleDef_HEAD_INIT,
    .m_name = "pcre.cpcre2",
    .m_doc = "Low-level bindings to the PCRE2 regular expression engine.",
    .m_size = -1,
    .m_methods = module_methods,
};

PyMODINIT_FUNC
PyInit_cpcre2(void)
{
    int match_lock_created = 0;
    int jit_lock_created = 0;

    if (PyType_Ready(&PatternType) < 0) {
        return NULL;
    }
    if (PyType_Ready(&MatchType) < 0) {
        return NULL;
    }

    PyObject *module = PyModule_Create(&moduledef);
    if (module == NULL) {
        return NULL;
    }

    PcreError = PyErr_NewExceptionWithDoc("pcre.PcreError",
                                          "Exception raised for PCRE2-related errors.",
                                          NULL,
                                          NULL);
    if (PcreError == NULL) {
        Py_DECREF(module);
        return NULL;
    }

    if (register_pcre_error_types(module) < 0) {
        goto error;
    }

    if (match_data_cache_lock == NULL) {
        match_data_cache_lock = PyThread_allocate_lock();
        if (match_data_cache_lock == NULL) {
            PyErr_NoMemory();
            goto error;
        }
        match_lock_created = 1;
    }

    if (jit_stack_cache_lock == NULL) {
        jit_stack_cache_lock = PyThread_allocate_lock();
        if (jit_stack_cache_lock == NULL) {
            PyErr_NoMemory();
            goto error;
        }
        jit_lock_created = 1;
    }

    Py_INCREF(&PatternType);
    if (PyModule_AddObject(module, "Pattern", (PyObject *)&PatternType) < 0) {
        Py_DECREF(&PatternType);
        goto error;
    }

    Py_INCREF(&MatchType);
    if (PyModule_AddObject(module, "Match", (PyObject *)&MatchType) < 0) {
        Py_DECREF(&MatchType);
        goto error;
    }

    Py_INCREF(PcreError);
    if (PyModule_AddObject(module, "PcreError", PcreError) < 0) {
        Py_DECREF(PcreError);
        goto error;
    }

#define ADD_INT_CONST(name) \
    if (PyModule_AddIntConstant(module, #name, name) < 0) { \
        goto error; \
    }

    ADD_INT_CONST(PCRE2_ANCHORED);
    ADD_INT_CONST(PCRE2_CASELESS);
    ADD_INT_CONST(PCRE2_DOTALL);
    ADD_INT_CONST(PCRE2_EXTENDED);
    ADD_INT_CONST(PCRE2_LITERAL);
    ADD_INT_CONST(PCRE2_MULTILINE);
    ADD_INT_CONST(PCRE2_NO_AUTO_CAPTURE);
    ADD_INT_CONST(PCRE2_UNGREEDY);
    ADD_INT_CONST(PCRE2_UTF);
    ADD_INT_CONST(PCRE2_UCP);

    ADD_INT_CONST(PCRE2_NOTBOL);
    ADD_INT_CONST(PCRE2_NOTEOL);
    ADD_INT_CONST(PCRE2_NOTEMPTY);
    ADD_INT_CONST(PCRE2_NOTEMPTY_ATSTART);
    ADD_INT_CONST(PCRE2_PARTIAL_HARD);
    ADD_INT_CONST(PCRE2_PARTIAL_SOFT);

#undef ADD_INT_CONST

    if (PyModule_AddStringConstant(module, "__version__", "0.1.0") < 0) {
        goto error;
    }

    if (PyModule_AddIntConstant(module, "PCRE2_CODE_UNIT_WIDTH", PCRE2_CODE_UNIT_WIDTH) < 0) {
        goto error;
    }

    return module;

error:
    Py_XDECREF(PcreError);
    PcreError = NULL;
    Py_CLEAR(PcreErrorByCode);
    Py_DECREF(module);
    if (jit_lock_created && jit_stack_cache_lock != NULL) {
        PyThread_free_lock(jit_stack_cache_lock);
        jit_stack_cache_lock = NULL;
    }
    if (match_lock_created && match_data_cache_lock != NULL) {
        PyThread_free_lock(match_data_cache_lock);
        match_data_cache_lock = NULL;
    }
    return NULL;
}
